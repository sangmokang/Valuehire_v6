# OMC / Claude Code 로컬 상태
.omc/
.claude/settings.local.json

# macOS
.DS_Store

# 의존성 / 빌드 산출물
node_modules/
dist/
build/
.next/
*.log

# 환경 변수
.env
.env.*
!.env.example

# 비밀 패턴 파일 (verify.sh 스캔 기준 — 로컬 전용, 절대 커밋 금지)
.secret-patterns

# 비공개 리뷰 결과 (원본 코드 재포함 가능 - 유출 방지)
.claude/private-reviews/
gptreview-*.md
verify-*.json

# 워크트리 (harness 작업 격리용, 커밋 대상 아님)
worktrees/
