# OMC / Claude Code 로컬 상태
.omc/
.claude/settings.local.json

# macOS
.DS_Store

# 의존성 / 빌드 산출물
node_modules/
dist/
build/
.next/
*.log

# 환경 변수
.env
.env.*
!.env.example

# 비밀 패턴 파일 (verify.sh 스캔 기준 — 로컬 전용, 절대 커밋 금지)
.secret-patterns
