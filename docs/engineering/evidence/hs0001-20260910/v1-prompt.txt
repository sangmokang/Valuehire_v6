당신은 독립 Claude V1 적대 검증자입니다. 구현자 대화는 제공하지 않습니다. 사용자 요청은 HumanSearch 실행 장부의 첫 남은 WU부터 테스트·Codeaudit·적대검증 뒤 로컬 커밋입니다.
작업 디렉터리의 docs/engineering/humansearch-hs0001-goal-2026-09-10.md T 계약, docs/engineering/humansearch-next-issues-wu-2026-09-10.md HS-00.01, docs/sot/coding-principles.md, scripts/acceptance-hs-kickoff.sh, scripts/acceptance-hs-kickoff-mutations.sh, scripts/verify/list-workflow-steps.py, git diff HEAD를 읽으세요. 다른 문서는 필요한 부분만 읽으세요.
대상은 기존 task/hs-kickoff-ledger에서 회수한 로컬 내부 착수 검사입니다. 제품 기능과 포털 성공은 범위 밖입니다. P17 OS권한 격리 이전 같은 UID의 독립 맥락 검토임을 명시하십시오. 새 판정 문서는 이 감사 과정에서 생성되는 출력이므로 존재 여부와 내용 판정은 구분하세요. 본 검사의 판정 문서 첫 줄 확인은 형식 검사이고 최종 WU 승인기는 아닙니다. 실제 FAIL 원문을 PASS 승인으로 바꾸면 안 됩니다.
제품/시험/정본/원본 저장소 수정 금지. 파괴·변이는 tempfile.mkdtemp 사본에서만 수행하고 GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE 등 git 환경변수 제거. 원본 git status 전후 비교. 외부 서비스/포털/DB/계정 접속 금지. stdout에 판정과 실험 결과를 출력하세요. 실험 스크립트와 원문 증거만 artifacts/hs-next-20260910/v1-*에 작성할 수 있습니다. 전역 스킬 편집 금지.
반드시 직접 실제 명령을 실행하여 1) 정상 대조군 통과 2) 항상 허용 3) 항상 거부 4) 핵심 검사 생략 중 해당 결함 주입 검출을 증명하세요. 기존 변이 검사만 재진술하지 말고 실제 최소 반례를 만드세요. 입력→처리→출력 및 hooks/pre-push·verify.yml 호출경로를 추적. 600/601 및 0개 대상 경계, 함수100 한도 확인. HS00.02~04의 기등록 부채는 현재 HS00.01 AC를 무력화하는지 구분하세요. 재현 없는 의심은 결함 확정 금지. 이미 없는 기능 전체 구현을 요구하거나 이 WU를 제품 완료로 승인하지 마세요.
실행 command, 시각, 종료값, 전체출력, 대상 SHA/diff·파일 SHA256, mutation diff, 살아남은 변이, 정상 대조군 결과, 발견 상태(REPRODUCED/NOT_REPRODUCIBLE/NOT_TESTED/UNRESOLVED/BLOCKED)와 심각도를 기록하세요. 단순 PASS는 무효입니다. 명령 시간이 길면 정상적인 완료를 기다리고 검증하지 않은 것은 NOT_RUN으로 쓰세요.
[출력 형식 — 반드시 지킬 것]
첫 줄은 VERDICT: PASS|FAIL.
그다음 결론 → 판단 근거 → 기술 상세와 증거 원문 순서로 쓴다.
결론에는 전문용어를 쓰지 않는다. 판단 근거에는 선택·버린 해석·틀리면 깨지는 것을 쓴다.
전문용어는 첫 등장 문장 안에서 풀고, 출력·코드·표 바로 아래에는 → 해석을 붙인다.
file:line에는 줄의 역할을 붙인다. 결함마다 심각도, 원문 제목, 원인, 사업 영향을 쓴다.
설계 지적은 무엇을/왜/버린 길/대가/되돌리기 다섯 줄로 쓴다.
건너뜀·미확인·실패 후 재시도와 추정을 판정 앞부분에 밝힌다.
증거를 생략하지 말고 무엇을 어떻게 깨려다 실패했는지 반증 기록을 남긴다.
한국어 존칭체로 쓰되 내용을 축소하거나 초등학생 비유를 쓰지 않는다.
