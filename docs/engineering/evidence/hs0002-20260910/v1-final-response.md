VERDICT: PASS

# HS-00.02 실제 Claude V1 최종 독립 적대 검증 판정

검증자: Claude V1(실제 도구 실행, 같은 UID 로컬 검토). OS/P17 영수증이 아닙니다. 대상은 HEAD `8bef581b…`(추가 RED) 위의 커밋 전 후보이며, 후보 다섯 파일의 SHA256은 시작·종료 시점 모두 `final-candidate-files.json`과 같습니다. 원문 판정은 `artifacts/hs0002-20260910/v1-final-verdict.md`, 근거 명부는 `v1-final-evidence.json`, 개별 실행은 `v1-final-adv-*` 파일에 있습니다.

## 결론

기대한 실패 사유가 실제 실패 줄에 그대로 있을 때만 성공으로 세고, 다른 사유 속 문구·정규식·성공 줄 미끼·빈 값·공백만·줄바꿈 섞인 값·읽을 수 없는 파일은 모두 거부합니다. 동결 시험 43개, 기존 변이 37건, 전체 회귀 262개가 통과했고, 격리 사본에 넣은 고장 24종은 전부 동결 시험의 실제 단언 실패로 잡혔습니다. 직접 입력 공격 93건과 실제 셸 경로 공격 21건에서 계약과 어긋난 판정은 없었습니다. 높음·중간 결함은 없습니다.

다만 구현자의 최종 실행 장부 가운데 전체 회귀 기록 하나가 존재하지 않는 스크립트 이름을 불러 실패로 끝나 있습니다. 제가 올바른 명령으로 같은 지문에서 직접 실행해 통과를 확인했으므로 제품 판정은 합격이지만, GREEN 커밋 전에 그 기록을 바로잡아야 합니다.

## 건너뜀·미확인·재시도·추정

- **재시도 1건(제 코드 오류, 제품 무관)**: 셸 경로 공격 스크립트 1차가 하위 폴더 없이 파일을 쓰려다 실패했습니다. 원문은 `v1-final-adv-negative-path-attempt1-failed.log`에 보존했습니다.
- **미실행(범위 밖)**: 원격 CI·push·병합, 실제 포털·DB·메시지·브라우저, HS-00.03 동형·보이지 않는 문자, HS-00.04 workflow 실행 의미, HS-00.05 앞 단계 환경, WU0-B 출력 진위 인증.
- **관찰**: 제 세션 도중 다른 세션이 후보 파일 세 개를 git index에 올렸습니다. 내용 지문은 전후 동일하며 저는 index를 건드리지 않았습니다. 구현자의 고장 주입이 동시에 실행 중이어서 그 산출물은 읽기만 했습니다.
- **추정**: "정규 출력"은 착수 검사기가 `PASS: `/`FAIL: `/`CHECKED: ` 행만 낸다는 사실(33·34행 정의, 109·111·113·217행 실패 문구)에 근거합니다.
- **제 코드**: 스크립트 6개는 파일 600줄·함수 100줄 한도를 통과했고, 같은 검사기로 600 허용/601 거부/100 허용/101 거부/대상 0개 거부를 실제 실행했습니다. 제 스크립트에는 ruff 권고 11건이 남아 있으나 제품·시험 파일은 ruff·mypy 모두 깨끗합니다.

## 판단 근거

- **선택한 해석**: 계약의 "FAIL 행의 정규 문구에 나타나면"을 "`FAIL: ` 바로 뒤에서 기대 문구로 시작하고 줄 끝·ASCII 공백·탭·슬래시로 닫힌다"로 읽었습니다. 짧은 사유 세 종류는 검사기가 내는 전체 문구를 정규식 전체 일치로 대조하는 것으로 읽었고, 제품이 정확히 이 해석입니다.
- **버린 해석**: "행 어디에든 있으면 인정"은 계약이 부분문자열을 거부하며 고장 m12에서 시험 3건이 실패했습니다. "행 완전 일치만"은 정상 상세를 과잉 차단하며 m20에서 8건이 실패했습니다. "공백만인 기대값 허용"은 2차 계약이 거부하며 m07·n26에서 5건·4건이 실패했습니다.
- **틀리면 깨지는 것**: 제가 정규 형식이라 부른 문구가 검사기 실제 출력과 다르면 기존 변이 37건이 빨개져야 합니다. 실제로 PASS 37/FAIL 0이었고 `행 없음` 3건·`대상 칸에 다른 대상` 1건·`처분이 둘 이상` 1건·`판정 문서 없음` 1건이 기대 사유로 차단됐습니다. 31개 음성 기대 문자열은 9종이며 빈 값·짧은 값이 없습니다.
- **단순 PASS가 아닌 근거**: 항상 허용 25건, 항상 거부 15건, 옛 grep 복원 25건, 셸 판정 반전 39건, 인수 순서 바꿈 14건, 따옴표 제거 15건, 판정기 종료값 무시 25건, 판정기 경로 오류 14건의 실제 단언 실패를 냈습니다. 모든 고장 로그에서 `NameError`·`SyntaxError`·`ImportError`·수집 오류는 0건입니다.

## 기술 상세와 증거

### 정상 대조군 (현재 지문에서 직접 실행)

| 실행 | 명령 | 시각(UTC) | 종료값 | 결과 |
|---|---|---|---|---|
| 정조준 | `humansearch/.venv/bin/python -m pytest -q …test_hs_0002.py …boundaries.py` | 22:46:48~54 | 0 | 43 passed |
| 정본 명령 | `cd humansearch && uv run --no-sync pytest -q tests/test_hs_0002.py tests/test_hs_0002_boundaries.py` | 22:50:44~51 | 0 | 43 passed |
| 기존 변이 | `bash scripts/acceptance-hs-kickoff-mutations.sh` | 22:47:03~45 | 0 | PASS 37 / FAIL 0 / CHECKED 37 |
| G2 | `bash scripts/acceptance-hs-gates.sh` | 22:50:17~40 | 0 | ruff·mypy 45파일 clean, 262 수집·통과, hs_0002 노드 43 |
| 정적 | ruff·mypy·`bash -n`·`git diff --check` | 22:50 | 0 | 모두 clean |
| 한도 | `v1-final-adv-budget.py` | 22:49 | 0 | 593/51/178/73줄, 함수 최대 31줄 |

→ 해석: 카드가 적은 정조준 20+23, 변이 37, G2 239+23이 현재 지문에서 그대로 재현됐습니다. 과거 지문의 결과를 승인 근거로 쓰지 않았습니다.

### 격리 사본 고장 주입 38종 (`v1-final-adv-mutants.py`, mktemp 사본, GIT_*/BASH_ENV/ENV 제거, 원본 무변경 확인)

원본 24종은 `final-g-mutants.py`를 읽고 제 실행기에 다시 구현했습니다. 정상 사본 m00은 43 passed였고 m01~m24는 모두 KILLED입니다. 실패 수는 m01 25, m02 15, m03 10, m04 2, m05 4, m06 25, m07 5, m08 5, m09 1, m10 4, m11 2, m12 3, m13 2, m14 2, m15 1, m16 3, m17 39, m18 14, m19 15, m20 8, m21 1, m22 15, m23 4, m24 1입니다.

→ 해석: 2차 검토에서 살아남았던 8종(m04·m07·m09·m11·m15·m16·m21·m24)이 새 경계 시험 23개로 모두 잡힙니다. 실패는 전부 종료값·접두사 단언이며 수집 오류가 아닙니다.

신규 13종은 7종 KILLED(기대값 strip, 공백 가드 약화, em-dash 경계 추가, UTF-8 오류 미포착, 불일치 exit 0, 판정기 종료값 무시, 판정기 경로 오류), 6종 SURVIVED이며 아래 정보 3에서 다룹니다.

### 직접 입력 공격 93건 (`v1-final-adv-cli-probes.json`)

문자 그대로(정규식 메타·백슬래시·`|`·`^$`·`*`), FAIL 행(접두 공백 없음·두 칸·소문자·들여쓰기·CHECKED 행 안·둘째 FAIL 행), 빈값·공백·탭·U+3000·NBSP, 양끝 공백 문자 그대로, 내부 CR/LF/CRLF, 경계(공백·탭·슬래시 허용, em-dash·하이픈·괄호·콜론·쉼표·VT·FF·CR 거부), CRLF 파일, CR 전용 파일, BOM, legacy 3종 변형 20여 건, 정상 상세, 빈 파일, UTF-8 오류·UTF-16·NUL, 파일 없음·디렉터리·인수 0/1/3개. 계약 기대와 불일치 0건입니다. 유일하게 표시된 `expected-zero-width-space`는 제 기대값 설정이 계약 정의와 달랐던 항목으로 정보 2로 분류했습니다.

### 실제 negative 셸 본문 경로 공격 21건 (`v1-final-adv-negative-path.json`)

동결 시험의 `call_negative`로 제품 `negative()` 본문을 실행했습니다. 미끼 뒤 진짜 FAIL 행 허용, 미끼만 거부, argv 줄바꿈·CR 거부, 셸 메타문자·`--`·`-n` 문자 그대로, legacy 정규/0개/맨몸/1개, 공백·빈 값, 덧붙임·em-dash·슬래시가 모두 기대대로였습니다. out.log 읽기 권한 제거와 저장소 루트가 아닌 cwd 실행도 `FAIL: … 사유가 다르다`로 닫혔습니다.

### 검사 자체에 대한 공격

- 동결 시험은 제품 함수 본문을 잘라 실제 bash로 실행하므로 함수명·표식이 바뀌면 예외로 시끄럽게 깨집니다.
- Hypothesis 시험은 `derandomize=True`라 실행마다 같은 예를 내며 m03·m05를 잡았습니다.
- 셸 호출은 기대값을 인용해 넘기고 helper 종료값 2·127을 모두 실패로 셉니다. `|| true`와 경로 오류 고장에서 각각 25건·14건이 실패했습니다.
- CI 배선은 `.github/workflows/verify.yml` 287행이며 스크립트가 루트로 이동한 뒤 상대 경로로 helper를 부릅니다. 실행 환경 검증은 HS-00.04/05 범위입니다.

## 결함·지적 목록

**D1 · 낮음(기록 결함, GREEN 전 정정 필수) · 최종 G2 실행 기록이 존재하지 않는 명령을 가리킨다**
- 위치: `artifacts/hs0002-20260910/final-g2.json` — 명령 `bash scripts/acceptance-g2.sh`, 종료값 127, 로그 내용 `No such file or directory`.
- 원인: 실제 G2 스크립트는 `scripts/acceptance-hs-gates.sh`인데 최종 실행에서 이름을 잘못 적었습니다.
- 사업 영향: 이 기록으로 GREEN을 확정하면 실행하지 않은 검증을 통과로 기록하는 것이 됩니다. 제품 자체는 제 `v1-final-adv-g2.json`으로 같은 지문에서 통과가 확인됐습니다.
- 요구: 올바른 명령으로 새 라벨에 다시 기록하고 카드·증거 폴더를 갱신한 뒤 GREEN 하십시오.

**정보 1 · 카드·증거 폴더가 최종 후보보다 앞선다** — 카드 57행의 "G2 239개"는 현재 262개이며, `docs/engineering/evidence/hs0002-20260910/`은 07:39 수집분이라 final 결과가 없습니다.

**정보 2 · 보이지 않는 문자만인 기대값은 허용된다** — U+200B·U+2060·U+FEFF·U+00AD 한 글자 기대값은 종료값 0입니다. 계약의 "공백 문자"는 Unicode White_Space이고 이 글자들은 그 밖이며, 31개 호출처는 상수라 영향이 없습니다. HS-00.03에서 함께 다루기를 권합니다.

**정보 3 · 살아남은 신규 고장 6종은 시험 강도 관찰이며 제품은 올바르다**

| 변이 | 제품 실제 동작(CLI 확인) | 비고 |
|---|---|---|
| n27 CR 접미 제거 삭제 | CRLF 파일 4건 정상 | 시험은 LF 파일만 씀 |
| n28 `FAIL:` 뒤 공백 없는 접두 인정 | `FAIL:CI …` 거부 | 사실상 등가 변이 |
| n29 legacy 불일치 시 문자 그대로 재시도 | 맨몸 legacy 3종 거부 | 시험에 맨몸 사례 없음 |
| n30 `행이 1개` 허용 | `행이 1개` 거부 | 시험에 1개 사례 없음 |
| n34 인수 개수 오류 exit 0 | 인수 0/1/3개 exit 2 | 시험 미포함 |
| n35 `splitlines()` 분할 | VT·FF 뒤 내용 포함해 거부 | 시험에 VT/FF 없음 |

→ 해석: 계약의 가짜 합격 조건 7종은 모두 시험에 고정돼 있고, 위 6종은 계약 문장 밖 세부입니다. P5에 따라 지금 동결 시험을 고치지 않고 다음 회귀 파일 후보로 남깁니다.

**설계 지적 · 짧은 기대값의 위험(정보)**
- 무엇을: 기대값 `CI`처럼 짧은 문구는 `CI 배선 불량`·`CI 스텝 불일치` 모두에 일치합니다.
- 왜: "기대 문구로 시작 + 경계"를 택해 상세 설명을 허용한 대가입니다.
- 버린 길: 행 완전 일치(정상 상세 차단), 최소 길이 강제(계약 밖 규칙).
- 대가: 호출처가 뜻 있는 문구를 쓴다는 규율에 의존합니다. 현재 9종은 모두 뜻 있는 문구입니다.
- 되돌리기: 짧은 문구가 실제로 들어오면 그 호출처를 고치거나 별도 WU에서 최소 길이 규칙을 추가합니다.

## 반증 기록

부분문자열·정규식 메타·PASS 행·둘째 줄·CHECKED 행 미끼로 거짓 합격을 만들려 했으나 모두 거부됐습니다. 공백·탭·U+3000·NBSP·양끝 공백·CR/LF 삽입으로 빈 사유 합격을 만들려 했으나 거부됐고 양끝 공백은 문자 그대로 대조됐습니다. legacy 3종을 대상 변형·0개·01개·1개·덧붙임·뒤 공백·하이픈 대시·문맥 포함으로 통과시키려 했으나 정규 형식만 통과했습니다. 파일 없음·디렉터리·UTF-8 오류·UTF-16·읽기 권한 없음·잘못된 cwd·인수 개수 오류로 초록을 만들려 했으나 모두 실패 쪽으로 닫혔습니다. 정상 상세를 과잉 차단시키려 했으나 모두 허용됐습니다.

GREEN 전 남은 일은 D1 기록 정정과 정보 1의 카드 숫자·증거 폴더 갱신, 커밋 후 지문 재대조입니다.
