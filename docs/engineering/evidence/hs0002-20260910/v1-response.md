VERDICT: PASS

## 결론

기대한 실패 사유가 실제 FAIL 줄에 그대로 있을 때만 성공으로 세고, 다른 사유 안에 끼어든 문구·정규식·PASS 줄 미끼·빈 값은 거부합니다. 기존 37건 시험, 새 시험 20건, 전체 회귀 239건이 현재 지문에서 모두 통과했습니다. 제가 격리 사본에 넣은 고장 24종 중 핵심 고장 16종은 고정된 시험이 잡았습니다. 살아남은 8종은 제품 오류가 아니라 시험이 해당 조항을 따로 못 박지 않은 것이며, 제품 원문에 직접 입력을 넣어 모두 올바르게 판정됨을 확인했습니다. 높음·중간 결함은 없습니다.

판정문은 `artifacts/hs0002-20260910/v1-verdict.md`, 근거 명부는 `v1-evidence.json`에 저장했습니다.

## 건너뜀·미확인·재시도

- **재시도 3건**, 모두 제 검증 절차 오류이며 제품과 무관합니다. 변이 37건 1차는 작업 폴더 오류로 스크립트가 실행되지 않아 루트에서 재실행했습니다. CLI 공격 스크립트 1차는 제 파일의 바이트 리터럴 문법 오류였습니다. negative 본문 경유 공격 1차는 시스템 python에 pytest가 없어 uv 가상환경 python으로 재실행했습니다. 실패 원문은 `v1-*-attempt1-failed.*`에 보존했습니다.
- **미실행(범위 밖)**: 원격 CI, 병합, 실제 포털/DB/메시지/브라우저, HS-00.03 동형 문자, HS-00.04 workflow 실행 의미, HS-00.05 앞 단계 환경, WU0-B 출력 진위 인증.
- **추정**: "정규 출력"은 검사기 `scripts/acceptance-hs-kickoff.sh`가 pass/failc/CHECKED로만 stdout을 낸다는 사실을 grep으로 확인한 것에 근거합니다.
- 이 검토는 같은 UID 로컬 검토이며 OS/P17/라이브 영수증이 아닙니다.

## 판단 근거

- **선택한 해석**: 계약의 "FAIL 행의 정규 문구에 나타나면"을 "`FAIL: ` 뒤에서 기대 문구로 시작하고 끝/공백/탭/슬래시 경계"로 읽었습니다. 짧은 사유 3종은 검사기 109/111/113행이 내는 전체 문구의 정규식 fullmatch로 읽었습니다. 제품이 정확히 이 해석을 구현합니다.
- **버린 해석**: "FAIL 행 어디에든 문구가 있으면 인정"은 계약이 명시적으로 거부하며, m12 변이로 넣었을 때 시험이 잡았습니다. "전체 행 완전 일치"는 정상 사유를 과잉 차단하며, m20 변이로 시험이 잡았습니다.
- **틀리면 깨지는 것**: 제가 정규 형식이라 부른 문구가 검사기 실제 출력과 다르면 37건 변이 실행이 빨개져야 합니다. 실제 37/37 PASS였고, 6개 대상 × 3개 legacy 정규 문구 18건을 CLI로 직접 넣어 모두 exit 0이었습니다.
- **단순 PASS가 아닌 근거**: 옛 grep 복원은 12건, 항상 허용 11건, 항상 거부 9건, 정규식 복원 8건, 경계 삭제 2건, 셸 인수 순서 바꿈 9건, 셸 따옴표 제거 9건의 실제 단언 실패를 냈습니다.

## 기술 상세와 증거

원본 다섯 파일 SHA256은 시작·종료 시점에 동일하며 candidate-files.json과 일치합니다. RED 커밋 `0263108`의 시험 파일과 현재 시험 파일은 같은 지문입니다.

**정상 대조군 (직접 실행)**

| 실행 | 종료값 | 결과 | 보존 |
|---|---|---|---|
| 정조준 pytest | 0 | 20 passed | v1-target-pytest.log |
| 기존 변이 스크립트 | 0 | PASS 37 / FAIL 0 / CHECKED 37 | v1-mutations-37.log |
| G2 게이트 | 0 | ruff·mypy clean, 239 수집·통과, hs_0002 노드 20 | v1-g2.log |
| ruff/mypy/bash -n/diff --check | 0 | 모두 clean | v1-static.log |
| 파일 600/함수 100 한도 | 0 | 593/51/178줄, 함수 최대 32줄, 600허용·601거부·0거부 | v1-budget.log |

→ 해석: 구현자 green 로그와 같은 결과를 현재 지문에서 제가 다시 얻었습니다. 과거 지문으로 승인하지 않았습니다.

**격리 사본 변이 24종** (mktemp 사본, GIT_*/BASH_ENV/ENV 제거, 원본 무변경)

| 변이 | 결과 | 실패 시험 수 |
|---|---|---|
| m00 정상 사본 | PASS | 20 passed |
| m01 항상 허용 / m02 항상 거부 | KILLED | 11 / 9 |
| m03 정규식 복원 | KILLED | 8 |
| m05 경계 삭제 / m20 전체 행 완전 일치만 | KILLED | 2 / 6 |
| m06 옛 grep 복원 (mutations.sh 189행) | KILLED | 12 |
| m08 legacy 부분문자열 / m10 legacy search / m13 대상 `.*` / m14 `/` 경계 삭제 / m23 legacy 표 비움 | KILLED | 3 / 3 / 2 / 1 / 3 |
| m12 행 중간 부분문자열+경계 / m22 접두 제거 생략 | KILLED | 3 / 9 |
| m17 셸 반전 / m18 인수 뒤바꿈 / m19 따옴표 제거 | KILLED | 20 / 9 / 9 |
| m04 FAIL 행 필터 삭제 / m21 PASS 행도 대조 | **SURVIVED** | 0 |
| m07 빈 기대값 가드 삭제 | **SURVIVED** | 0 |
| m09 legacy `re.match` / m11 경계를 비영숫자 전체로 | **SURVIVED** | 0 |
| m16 파일 못 읽으면 exit 0 | **SURVIVED** | 0 |
| m15 개행 가드 삭제 / m24 legacy 개수 0 허용 | SURVIVED (동등 변이) | 0 |

→ 해석: 카드가 정의한 가짜 합격 조건은 모두 잡힙니다. 살아남은 변이는 아래 결함 항목으로 분류했습니다. diff와 전체 출력은 `v1-mutant-<이름>.diff/.log`에 있습니다.

**제품 원문 직접 공격** (CLI 63건, negative 본문 경유 13건)

- CLI 입력 오류: 인수 1개/4개, 파일 없음, 디렉터리, 깨진 UTF-8 → 모두 exit 2. 셸이 `!`로 감싸므로 실패로 닫힙니다.
- FAIL 행 시작: 공백 없는 `FAIL:`, 앞 공백, 소문자, BOM → 거부. 뒤쪽 줄의 FAIL 인정, CRLF 허용.
- 문자 경계: `불량XYZ`·`불량—상세`·`불량-상세` 거부. 탭·줄 끝·`없음/빈 파일` 인정.
- 정규식 메타: 기대 `.*`는 아무 행에도 안 맞고, `^…$`·`[CI]`는 문자 그대로만 인정, `A|B`는 거부.
- 빈값/개행: 빈 기대값, 개행·CR 포함 기대값 거부. `FAIL: `(빈 메시지)+빈 기대값도 negative 본문에서 FAIL·CHECKED 1.
- legacy 3형식: 6개 대상 × 3문구 정규 형식 18건 인정. `PR #`·공백 포함 브랜치·`행이 1개`·`다른 대상 0개`·끝 잘림·`행 없음XYZ`·앞 미끼 거부.
- 미끼: PASS만 있는 로그, `CHECKED: CI 배선 불량` 줄, 접두 없는 생 줄 → negative 본문에서 모두 FAIL.
- 실제 검사기 문구 6종 → PASS 한 건·CHECKED 1·exit 0. `PR #54` 사유에 `PR #13` 기대 → FAIL.

→ 해석: 계약의 합격·거부 조건을 제품 원문이 그대로 만족합니다. 예상과 다른 1건은 F4입니다.

**결함 목록**

**F1 · 낮음 · FAIL 행 제한과 빈 기대값 가드가 시험으로 독립 고정되지 않음**
- 위치: `humansearch/tests/test_hs_0002.py:104` PASS 미끼 사례의 미끼 문구가 줄 시작이 아닙니다. `:116` 빈 기대값 사례는 경계 검사만으로도 거부됩니다.
- 원인: 접두 대조가 PASS 줄을 이미 배제하고 경계 검사가 빈 기대값을 이미 거부해 helper 22·25행이 시험상 잉여가 됩니다. 구현자도 g-retry-explanation.md에 같은 무효과를 기록했습니다.
- 사업 영향: 제품은 정상입니다. 향후 리팩터에서 두 줄이 사라져도 시험이 침묵하는 시험 강도 부채입니다.

**F2 · 낮음 · legacy 끝 경계와 경계 문자 집합이 시험으로 고정되지 않음**
- 위치: `humansearch/tests/test_hs_0002.py:155` bait는 앞 미끼만, `:173` 확장 알파벳이 영숫자만입니다.
- 원인: fullmatch→match와 경계 확대가 시험 입력 공간 밖입니다.
- 사업 영향: 제품은 정상입니다. 시험 강도 부채.

**F3 · 낮음 · 파일 누락/입력 오류 경로가 시험으로 고정되지 않음**
- 위치: `scripts/verify/has-kickoff-failure.py:39` 이하 usage/읽기 오류 exit 2. 시험 harness가 항상 out.log를 씁니다.
- 원인: 시험이 helper CLI를 직접 호출하지 않습니다.
- 사업 영향: 제품은 정상이며 run_target이 항상 out.log를 쓰므로 실제 발현 가능성이 낮습니다.

**F4 · 정보 · 공백만 있는 기대값이 빈 값으로 거부되지 않음**
- 위치: `scripts/verify/has-kickoff-failure.py:22` 빈 문자열만 검사합니다.
- 원인: 기대 `" "`와 메시지 `"  x"`는 startswith와 경계 검사를 모두 통과합니다.
- 사업 영향: 31개 호출은 모두 비공백 상수라 영향 없음.

**F5 · 정보(설계) · 접두 대조의 짧은 기대값**
- 무엇을: 기대 `CI`가 두 다른 사유 앞에서 모두 인정됩니다.
- 왜: 계약이 "기대 문구로 시작+경계"를 요구하고 31개 문자열 유지를 명시합니다.
- 버린 길: 전체 행 완전 일치는 정상 사유를 과잉 차단합니다.
- 대가: 기대 문자열이 사유 전체를 담아야 구분력이 생깁니다.
- 되돌리기: 별도 WU에서 기대 문자열을 더 길게 바꾸면 되며 이 WU 범위 밖입니다.

**반증 기록**: 부분문자열, PASS/CHECKED 미끼, 정규식, 경계 없는 확장, legacy 앞/뒤 미끼, 셸 인용 깨기, 인수 뒤바꿈, 잘못된 대상 번호로 거짓 합격을 만들려 했으나 제품 원문은 전부 거부했습니다. 과잉 차단을 만들려는 시도(실제 문구 6종, CRLF, 탭, 줄 끝, 슬래시, legacy 18건)도 모두 인정됐습니다.

**보존 경로**: artifacts/hs0002-20260910/ 아래 v1-* 79개 파일. 명령·시각·종료값·전체 출력·지문·diff·실패 재시도를 모두 담았습니다.
