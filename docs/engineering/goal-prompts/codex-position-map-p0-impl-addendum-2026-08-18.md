# P0 구현 지시 보완판 (2026-08-18, 3차 투입) — 아래 본문이 원 지시서보다 우선한다

이 파일 뒤에 원 지시서(`codex-position-map-p0-impl-2026-08-18.md`)가 그대로 이어 붙는다. 충돌 시 **이 보완판이 이긴다.** 1·2차 투입이 S0에서 멈춘 원인(환경) 두 가지를 사람이 해결했고, 규칙 하나를 현실에 맞게 고쳤다.

## 바뀐 것 3가지

**1. S0의 워크트리·환경은 이미 준비되어 있다 — 새로 만들지 마라.**
- 네 작업 폴더(workspace)가 곧 워크트리다: `~/Desktop/Valuehire_v4/worktrees/position-map-p0`, 브랜치 `task/position-map-p0` (main 8a20289e 기점).
- `.env.local` 복사와 `node_modules` 링크도 완료. 시작 자격 검사(red-ledger)는 RED 0/34로 통과 확인됨(2026-08-18, 검증자 실행).
- 따라서 너는 **S0을 "기준선 숫자 기록"만 수행하고 S1부터 시작**한다. `git worktree add`·clone·`npm install` 금지(이미 있음). 커밋·브랜치 작업은 이 폴더에서 그대로 하면 된다(저장소 관리 폴더 쓰기가 허용된 구성으로 실행 중이다).

**2. 전진 조건 변경: "전체 초록" → "기준선 대비 악화 0".**
- main 자체가 전체 초록이 아님이 실측됐다(기준선 실패 존재 — 이 작업과 무관한 기존 실패). 기준선 실패 전문은 워크트리 안 `docs/engineering/p0-baseline-verify-20260818.txt`에 고정되어 있다(검증자가 저장). 네 S0 임무는 이 파일에서 **실패한 시험 파일 목록을 추출해 보고서에 표로 기재**하는 것이다.
- 각 스텝의 전진 조건은 이제: ① 그 스텝의 새 시험 전부 초록 ② `npm run verify` 재실행 결과, **기준선에서 초록이던 것 중 새로 빨개진 것 0건** ③ 기준선 실패 목록에 없던 새 실패 0건. 기준선 실패 자체를 고치는 것은 **범위 밖** — 건드리지 마라.
- AC-P0-6도 같은 의미로 읽는다: "기준선에서 초록이던 기존 시험 전부가 끝에도 초록".
- §F push 절차의 1번(최종 전체 재시험)도 같은 기준을 적용한다: 기준선 대비 악화 0이면 push 진행.

**3. 보고서에 추가로 적을 것.**
- 기준선 실패 목록(시험 파일 단위)과 "이 실패들은 착수 전부터 존재했고 건드리지 않았다"는 확인.
- 매 스텝 verify의 실패 목록이 기준선 목록과 동일했음을 증명하는 대조(다르면 그 시점에 중단하고 기록).

나머지 — 금지 목록(§A), 스텝 체인(§D), 인수 기준(§E), push 절차(§F), 보고서(§G), 채점표(§H) — 는 원 지시서 그대로다. 아래에 원 지시서 전문이 이어진다.

---
