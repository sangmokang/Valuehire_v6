VERDICT: FAIL

## 판정 요약

T의 문언 요건(하나의 WU=하나의 주장 / WU별 AC+반증1~3+완료커밋 / 전체 strict→codeaudit→결합 적대검증→PR→CI→GREEN→MERGE / P5 RED 선행 / squash 경계 구분 / 일반 WU 비강제 / 고위험 독립리뷰)은 **문구 수준에서는 모두 배치되어 있다.** 그러나 이 PR이 스스로 정본화한 규칙("표적 AC 실행 + 반증 1~3 통과 후에만 완료 커밋으로 닫는다")을 **이 PR 자신이 지키지 못한 상태로 SOT 두 파일을 이미 변경**했고, AC-1/AC-2의 검증 명령이 구조적으로 가짜 합격을 허용한다. 방법론 문서의 신뢰성은 그 문서가 자기 절차를 통과했는지에 전적으로 의존하므로 병합 불가로 판정한다.

전제 한계: 명령을 직접 재실행하지 못했으므로 아래 C1·M1의 종료값 판정은 `rg` 표준 동작(대체 패턴 중 **하나라도** 매치하면 exit 0)에 근거한 추론이다. 이 추론이 틀리려면 저장소가 `rg`를 래핑해 전 패턴 AND 매칭으로 바꿔야 하는데, 그런 래퍼는 제공 원문 어디에도 선언되어 있지 않다.

---

## 결함

### C1 — [Critical] AC-1 검증 명령이 RED에서도 통과한다: 기록된 RED 종료값과 고정 명령이 불일치

- **file:line**: `docs/engineering/work-unit-methodology-goal-2026-08-21.md:80-81`, 증거 블록 `:107-112`
- **원인**: 고정 명령은 `rg -n "Work Unit|하나의 주장|완료 커밋|squash" docs/sot/git-workflow.md` 단일 호출이고 기대값은 "종료값 0이며 **네 개념이 모두**"이다. `rg`의 대체 패턴은 OR이므로 네 개 중 하나만 있어도 exit 0이다. 그리고 이 PR이 첨부한 RED 로그 자신이 `PRESENT: squash`(:111)를 기록한다 — 즉 변경 **이전**에도 `squash`는 이미 `git-workflow.md`에 존재했고, 고정 원명령을 그대로 돌렸다면 RED 시점의 종료값은 **0**이어야 한다. 그럼에도 로그는 `AC-1_RED_EXIT=1`(:112)로 적혀 있다. 결론은 둘 중 하나다: (a) 실제로는 goal에 고정한 원명령이 아니라 패턴별 개별 검사라는 **다른 하니스**를 돌리고 그 결과를 AC 증거로 제시했거나, (b) 종료값이 사실과 다르게 기록되었다. 어느 쪽이든 "goal에 고정한 해당 AC의 원명령과 기대 종료값을 실제로 실행한다"(`docs/sot/verification-commands.md:38`)는 이 PR이 방금 정본화한 계약을 위반한다.
- **사업영향**: RED가 "올바른 이유로 실패했다"는 주장(:122)의 근거가 무너진다. 이 방법론은 전 조직의 분해·커밋·검증 순서를 규정하는 L3 변경이므로, 방법론 도입 PR 자체가 위조 가능한 RED 증거를 정본 사례로 남기면 이후 모든 팀 작업이 동일한 우회(원명령 대신 편한 대체 하니스 실행)를 정당화한다. 검증 체계의 신뢰가 상수 배로 손상된다.

### C2 — [Critical] 장부는 WU-01/WU-02 모두 OPEN, 적대검증 로그는 공란인데 SOT 정본 변경이 이미 diff에 포함됨

- **file:line**: `docs/engineering/work-unit-methodology-goal-2026-08-21.md:96-97`(상태 OPEN), `:142-144`(적대 검증 로그 "아직 실행 전입니다"), `:136-140`(Harness R2·R4·V1·V2 미실행) vs 실제 변경 `docs/sot/git-workflow.md:17-55`, `docs/sot/verification-commands.md:19-61`
- **원인**: 새 규칙은 "일반 Work Unit은 `IMPLEMENT → 해당 AC 실행 → 반증 1~3개 → 완료 커밋`"(`verification-commands.md:46`)이고, 고위험 경계는 **독립 REVIEW를 추가**하며 "실행할 수 없으면 `NOT_RUN`이며 고위험 Work Unit을 PASS로 닫지 않습니다"(`:56`)라고 못박는다. 그런데 본 PR은 스스로 위험등급 **L3**(goal:42)을 선언하고도, R2(고장 사본 주입: "최종 검사 삭제", "파일 단위 WU", "모든 WU full audit"), V1, V2를 하나도 실행하지 않은 채 SOT 본문을 확정했다. 장부 상태도 OPEN 그대로여서 장부와 작업 트리가 불일치한다.
- **사업영향**: 첫 적용 사례가 "반증 없이 완료 커밋을 닫아도 된다"는 선례가 된다. 제공된 증거(check-docs-sot exit0, principles 32/32, mutation 40/40, pre-push 21개 exit0)는 전부 **저장소 전역 게이트**이지 AC-1·AC-2의 표적 반증이 아니다. 즉 이 PR은 자신이 새로 만든 "두 층 검증" 중 **아래층이 통째로 비어 있는 상태**로 병합을 요구하고 있으며, 이는 정확히 이 문서가 없애겠다고 선언한 실패 양식이다.

### M1 — [Major] AC-2 검증 명령도 동일한 OR 매칭 — 6개 필수 문구 중 1개만 남아도 영구 PASS

- **file:line**: `docs/engineering/work-unit-methodology-goal-2026-08-21.md:88-89`
- **원인**: `rg -n "LOCAL VALIDATE|작은 적대검증|전체 strict|전체 codeaudit|전체 적대검증|GitHub verify CI" docs/sot/verification-commands.md` 역시 하나만 매치해도 exit 0이다. 현재는 여섯 문구가 모두 존재하지만(`verification-commands.md:24-35, 38, 40, 42`), 이 AC는 향후 누군가 "전체 codeaudit"이나 "GitHub verify CI" 단계를 삭제해도 **탐지하지 못한다.** goal이 스스로 적은 counter-AC "최종 통합 검사를 삭제"(:90)를 이 명령으로는 잡을 수 없다 — counter-AC와 검증 명령이 정조준되어 있지 않다.
- **사업영향**: 회귀 방지 게이트로 위장한 무효 게이트가 SOT에 상주한다. 최종 CI/적대검증 단계가 조용히 삭제되는 경로가 열리며, 이는 T가 명시한 "결합 적대검증 → CI GREEN → MERGE" 보장이 종이 위에서만 유지된다는 뜻이다.

### M2 — [Major] 신규 12단계 순서와 기존 6단계 표·pre-push 재실행의 관계가 미정의

- **file:line**: `docs/sot/verification-commands.md:23-35`(신규 1~12단계) vs `:16-17`(기존 "5 — 배송" 행: `git push` 시 `hooks/pre-push`가 verify.sh + acceptance-*.sh 전량 재실행)
- **원인**: 신규 순서는 7~9(전체 strict/codeaudit/적대검증) → 10 PR → 11 CI로 적었지만, 실제 저장소에서 PR을 만들려면 그 사이에 반드시 `git push`가 있고 그 시점에 pre-push가 전량 재실행된다. push가 몇 번 단계인지, 7~9를 로컬에서 별도로 돌려야 하는지 아니면 pre-push 결과로 갈음되는지, WU 완료 커밋들을 중간에 push해도 되는지가 어디에도 없다. 기존 표는 그대로 남아 두 절차가 병존한다.
- **사업영향**: 실행자가 7~9를 "어차피 pre-push가 돌린다"며 건너뛰는 해석이 문언상 가능하다. 특히 `전체 codeaudit`(논리·구조·중복·복잡도 검토)은 pre-push가 대신할 수 없는 항목인데, 이 모호성이 그것을 상시 누락시킨다. T가 요구한 "모든 WU 뒤 전체 strict→codeaudit→결합 적대검증" 보장이 운영상 깨진다.

### M3 — [Major] 검증 가능한 상한("PR = 인수 기준 1개")을 비측정 문구로 교체

- **file:line**: `docs/sot/git-workflow.md:52`(`PR = 사용자 결과 1개`), `:44`(`오너가 반드시 볼 판단 항목이 6개 이상이면 목표와 PR을 나눈다`), `:51`(수명 24~48h 초과 = "목표나 Work Unit 분해가 너무 크다는 신호")
- **원인**: 기존 규칙 "PR = 인수 기준 1개"는 셀 수 있는 제약이었다. 대체된 "사용자 결과 1개"와 "판단 항목 6개"는 둘 다 정의가 없다. "판단 항목"이 AC인지, WU인지, 변경 파일인지, 오너 판단이 필요한 설계 결정인지 문서 어디에도 없고 계산 절차도 없다. 임계값 6의 근거도 제시되지 않았다.
- **사업영향**: PR 크기 상한이 사실상 해제된다. 이 SOT가 명시적으로 근거로 드는 v4 실측 사고(워크트리 77개·미병합 브랜치 113개, `:51`)의 재발 방향으로 규칙이 완화되며, "오너가 diff를 실제로 읽는다"는 P11 근거(`:55` 자동 병합 금지)가 무력화된다. 리뷰 누락으로 인한 결함 유입 비용이 직접 증가한다.

### L1 — [Minor] 문체 혼용 — 동일 SOT 파일 내 평서체/존댓말 혼재

- **file:line**: `docs/sot/verification-commands.md:56, 60`(“…재실행합니다 / …않습니다”) vs 같은 파일 `:38, 40, 42, 46, 48`(“…묻는다 / …않는다”), `docs/sot/git-workflow.md:17-55`(전면 평서체)
- **원인**: goal 원문에서 그대로 옮겨 붙이며 종결어미를 통일하지 않았다.
- **사업영향**: 낮음. 다만 SOT 문서 규약 검사가 문체를 포함하도록 확장될 경우 회귀 원인이 되고, 규범 문서의 권위/가독성을 떨어뜨린다.

### L2 — [Minor] 고위험 경로 목록 이중 정의(드리프트 원)

- **file:line**: `docs/engineering/work-unit-methodology-goal-2026-08-21.md:70` ↔ `docs/sot/verification-commands.md:50-54`
- **원인**: 동일한 고위험 경계 목록(`.github/workflows/**`, `hooks/**`, `scripts/acceptance-*`, `verify*`, `mechanism-registry`, 비밀·데이터, 배포·인증)이 두 문서에 각각 하드코딩되어 있고, 어느 쪽이 정본인지 선언이 없으며 동기화 검사도 없다.
- **사업영향**: 낮음~중간. 한쪽만 갱신되면 고위험 경로가 독립 REVIEW 없이 일반 WU로 닫힐 수 있다. T가 강화 대상으로 지정한 CI/훅/acceptance/verify/mechanism/비밀/데이터/배포/auth 전 항목이 이 드리프트에 노출된다.

### L3 — [Minor] goal 내 SOT 라인 참조가 본 PR 적용 후 이미 어긋남

- **file:line**: `docs/engineering/work-unit-methodology-goal-2026-08-21.md:33-34`가 `docs/sot/git-workflow.md:15`(작업 1개=인수 기준 1개)와 `:21`(PR=인수 기준 1개)을 지목하나, 적용 후 해당 내용은 각각 `git-workflow.md:17`, `:52`로 이동
- **원인**: "현재 상태" 서술이라 변경 전 기준일 수 있으나, 문서에 그 기준 시점이 명시되어 있지 않다.
- **사업영향**: 낮음. 후속 독자가 근거를 추적할 때 잘못된 줄을 읽는다.

---

## 깨뜨리려 했으나 깨지지 않은 지점 (T 요건 대조)

1. **squash 롤백 경계 과장** — 가장 흔한 오류이므로 우선 공격했다. `git-workflow.md:53`과 goal:21이 "WU 완료 커밋은 squash 전 검토·원인 격리 경계, squash 뒤 main 롤백 경계는 PR 전체 커밋, 특정 WU만 되돌릴 때는 역적용 새 커밋"으로 정확히 서술한다. 나아가 AC-1 counter-AC(:82)가 "squash 뒤 개별 커밋 revert 가능하다고 기록"을 명시적 가짜 합격으로 지정했다. T와 완전 일치.
2. **P5 RED 선행커밋 침식** — "WU 하나 = 커밋 하나"라는 외부 제안이 P5를 잠식하는지 확인했다. goal:19와 `git-workflow.md:36-38`이 "P5의 시험 불변 계약이 Work Unit보다 우선한다", "RED 계약을 먼저 별도 커밋", "완료 커밋은 기대값·skip·의존성을 바꾸지 않는다", "새 반례는 별도 RED 커밋 선행"까지 명시하며 외부 제안을 명시적으로 거부(:19)했다. 침식 없음.
3. **일반 WU에 외부 의존 강제** — `verification-commands.md:46`("모든 작은 UI·문서 변경에 full codeaudit나 외부 Agent를 강제하지 않는다"), `:56`("필수 외부 서비스로 고정하지 않습니다"), `git-workflow.md:45-46`("Agent 교체는 선택", "외부 모델·유료 서비스·별도 오케스트레이터가 없어도 기본 절차는 중단되지 않는다"), goal:7·비범위(:44, GitHub Pro/branch protection/CODEOWNERS 제외)가 일관되게 방어한다. 유료·외부 종속 삽입 시도는 실패.
4. **최종 관문 대체** — "WU 검사로 최종 검사를 대체"하는 해석 여지를 찾았으나, goal:15, :59, `verification-commands.md:42`("셋은 질문이 다르므로 서로의 PASS를 대신하지 않는다"), `:60`("Work Unit PASS만으로 PR을 만들거나 병합 완료를 주장하지 않습니다")가 3중으로 차단한다. 대체 해석 불가.
5. **파일 단위 WU 분해로의 퇴행** — `git-workflow.md:32`가 `WU-01 = verify.yml 수정` 같은 파일 분해를 반례로 직접 제시하고 goal:69가 "파일 수·줄 수로 나누지 않는다"로 못박는다. 퇴행 경로 없음.
6. **고위험 목록 커버리지** — T가 열거한 CI/훅/acceptance/verify/mechanism/비밀/데이터/배포/auth 9종이 `verification-commands.md:50-54`에 빠짐없이 대응한다(누락 항목 탐색 실패). 다만 이중 정의 문제는 L2로 별도 기록.

---

## 병합 조건

1. AC-1/AC-2 검증 명령을 **패턴별 개별 검사 후 전건 AND**로 재정의하고, goal에 고정한 그 명령 **원문 그대로**의 RED 종료값과 GREEN 종료값·전체 출력을 각각 첨부할 것.
2. Harness R2, R4, V1, V2를 실제 실행하고 적대 검증 로그와 장부 상태를 갱신할 것.
3. 신규 12단계에 `push`/pre-push 위치를 명시하고 기존 6단계 표와의 관계를 확정할 것.
4. "판단 항목"의 정의와 계수 방법을 명시하거나 검증 가능한 대체 상한을 복원할 것.
