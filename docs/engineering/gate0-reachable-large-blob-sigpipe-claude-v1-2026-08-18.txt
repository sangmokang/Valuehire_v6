VERDICT: PASS

# 결론

1. 이번 변경은 실제로 존재했던 결함(50MiB 크기의 대용량 기록 조각 앞부분에 금지된 값이 있어도 검사가 이를 "값 없음"으로 오판해 통과시키는 문제)을 코드 두 곳(로컬 판정 스크립트, 서버 자동검사 워크플로) 모두에서 고쳤습니다. 제가 이 문제를 재현하는 최소 실험을 직접 만들어 실행해본 결과, 수정 전 방식은 실제로 종료 코드 141(비정상 신호 종료)을 반환하며 값이 있어도 통과했고, 수정 후 방식은 종료 코드 0으로 정상 동작하며 정확히 차단했습니다. 코드 자체는 문제없이 동작합니다.

2. 시험 코드(20개 사례를 강제 실행하는 스크립트)를 실제로 돌려본 결과 20개 사례 모두 예정된 결과(정상 통과 또는 예정된 실패 문구로 차단)를 그대로 냈습니다. 기존에 보호하던 항목들(작은/큰 객체, 커밋 설명문, 파일경로, 태그, 도구 자체가 죽는 경우, 알 수 없는 객체 종류, 종료 후 정리 상태 검사, 검사 스크립트가 실행되는 저장소 밖의 다른 저장소를 건드리지 않는지)도 전부 그대로 유지되어 있습니다.

3. 이번 변경의 이력 기록(어떤 커밋이 무엇을 했는지)을 추적한 결과, "실패하는 시험을 먼저 고정하고 그 시험 파일은 이후 절대 손대지 않는다"는 원칙이 실제로 지켜졌습니다. 처음 실패를 고정한 커밋과 현재 커밋 사이에서 시험 파일 자체는 글자 하나도 바뀌지 않았습니다.

4. 이번 변경은 GitHub 이슈 #27(이번 작업 대상)에만 해당하는 범위이며, 별도로 열려 있는 이슈 #22(커밋 설명문·파일경로 안의 금지값을 도달 가능한 경로에서 추가로 잡는 작업, 아직 미착수)나 HumanSearch라는 별개 제품의 파일과는 전혀 섞이지 않았습니다. 실제로 변경된 파일 5개를 모두 확인했으며 그 범위를 벗어난 항목은 없습니다.

5. 세션 상태 점검에서 "20개 중 1개 실패"가 보고되는 현상을 직접 원인 추적했습니다. 이 실패는 이번 작업과 무관한 별도 작업(같은 컴퓨터에서 진행 중인 문서 정리 작업)이 만든, 아직 원격 저장소에 올리지 않은 커밋 2개 때문에 로컬의 `main` 브랜치가 원격과 어긋나 있어서 생기는 현상입니다. 이번 검토 대상인 큰 객체 결함 수정과는 관계가 없습니다. 다만 원인이 되는 커밋 개수는 문서가 암시한 "1개"가 아니라 정확히 "2개"였다는 점은 바로잡아 기록해 둡니다. 그리고 관련 작업 기록 문서는 "서버 쪽 검사까지 성공을 확인하기 전에는 다음 작업을 시작하지 않는다"고 스스로 못박아 놓았을 뿐, "배송이 끝났다"고 주장하는 곳은 어디에도 없었습니다. 오히려 문서 뒷부분에 "적대적 검증 결과"와 "전체 게이트 통과 기록"을 채워 넣을 자리를 비워둔 채로 남겨 두었는데, 이는 해당 절차가 아직 이 시점까지는 실행되지 않았다는 뜻이며, 정직하게 미완료를 표시해 둔 것으로 보입니다(과장된 완료 주장은 없었습니다).

6. macOS 특유의 오래된 bash(버전 3.2), 파이프 중간 명령의 종료 코드를 따로 잡아내는 기법(PIPESTATUS), 파이프 전체 실패 처리 옵션(pipefail), 따옴표 처리, 대용량 객체 처리 방식을 모두 직접 실행해 검증했지만 등급 "중간" 이상의 결함은 발견하지 못했습니다.

# 판단 근거

가장 핵심적인 갈림길은 "이 수정이 실제로 문제를 없앴는가, 아니면 겉으로만 그럴듯해 보이는가"였습니다. 이를 확인하기 위해 저는 문서의 서술을 그대로 믿는 대신, 수정 전 코드 패턴과 수정 후 코드 패턴을 각각 별도의 임시 저장소에서 직접 만들어 실행해 비교했습니다(아래 기술 상세의 "직접 재현 실험" 참조). 그 결과 수정 전 패턴이 실제로 종료 코드 141(끊어진 파이프 신호)을 내며 "값을 찾았음에도 실패로 보고하지 않는" 것을 직접 눈으로 확인했고, 수정 후 패턴은 이 문제가 사라진 것을 확인했습니다. 이것이 이번 판정에서 가장 신뢰도 높은 증거입니다 — 문서의 주장이 아니라 제가 독립적으로 재현한 사실이기 때문입니다.

두 번째 갈림길은 "시험 스크립트가 실제로 로컬 검사와 서버 검사 본문을 실행하는가, 아니면 흉내만 내는가"였습니다. 시험 스크립트를 읽어 보니 서버 검사 부분은 `.github/workflows/verify.yml` 파일에서 실제 실행 문구를 `awk`로 그대로 뽑아내 `bash -c`로 돌리는 방식이었습니다(고정 문자열을 베껴 쓴 별도 사본이 아님). 제가 직접 20개 사례를 전부 실행해 이 방식이 정말로 동작하고 예정된 실패 문구까지 정확히 맞아떨어지는 것을 확인했으므로, "실제 경로를 검사한다"는 주장을 받아들였습니다.

세 번째 갈림길은 "세션 상태 점검의 실패 1건을 이번 변경의 결함으로 볼 것인가, 무관한 환경 문제로 볼 것인가"였습니다. 저는 이걸 문서 서술을 믿지 않고, 실패한 검사 스크립트가 정확히 무엇인지 하나하나 직접 실행해 걸러냈습니다. 그 결과 원인은 로컬 `main` 브랜치가 별도 작업의 커밋 2개만큼 원격과 어긋나 있다는, 이번 작업과 전혀 무관한 로컬 환경 상태였습니다. 만약 이 판단이 틀렸다면(즉 이 실패가 실은 이번 변경 탓이라면) 이번 코드 자체에 아직 잡히지 않은 결함이 남아 있다는 뜻이 되므로 PASS 판정이 뒤집힐 수 있습니다 — 그래서 반드시 직접 원인을 추적해 확인했습니다.

버린 해석: "GitHub Actions 서버(Ubuntu 리눅스)에서도 정확히 같은 방식으로 동작할 것"이라는 부분은 로컬(macOS)에서 추출한 동일 스크립트 본문을 실행해 검증했을 뿐, 실제 GitHub 서버에서 그 워크플로를 돌려보지는 않았습니다. 다만 이번 수정이 의존하는 셸 동작(파이프 중간 명령 종료 코드 추적, 텍스트로 강제 취급하는 옵션)은 macOS와 리눅스에서 동일하게 표준적으로 지원되는 기능이라, 확인하지 못한 부분이지만 위험도가 낮다고 판단했습니다(※ 표시로 아래에 명시).

# 확인하지 못했거나 건너뛴 것 (판정 앞부분 명시)

- ※ GitHub Actions 서버(Ubuntu 러너)에서 실제로 워크플로를 돌려 성공을 확인하지는 못했습니다. 로컬(macOS)에서 워크플로 본문을 추출해 실행하는 방식으로만 검증했습니다. 이는 시험 스크립트 자체가 채택한 검증 방식과 동일합니다.
- ※ 이슈 #19라는 번호가 SOT 문서에 언급되어 있으나 `gh issue view 19`로 직접 조회하지는 않았습니다(#22, #23, #27만 조회). 다만 문서 문맥상 기존에 있던 AC-19라는 인수 기준의 번호로 보이며 새로 생긴 이슈 번호가 아닙니다.
- 세션 상태 점검(session-status.sh) 전체 실행에는 수 분이 걸렸습니다(저장소 전체 인수 스크립트 20개를 순차 실행). 완료까지 기다려 결과를 확인했습니다.

# 기술 상세

## 1) 변경 범위 확인 (섞임 없음)

```
$ git diff --stat origin/main..HEAD
 .github/workflows/verify.yml                                                          |  26 +-
 docs/engineering/gate0-reachable-large-blob-sigpipe-goal-2026-08-18.md                | 399 +++++++++++++++++++++
 docs/sot/verification-commands.md                                                     |   4 +-
 scripts/acceptance-0-2-unreachable-content.sh                                         | 156 ++++++--
 scripts/acceptance-0-2.sh                                                             |  29 +-
 5 files changed, 583 insertions(+), 31 deletions(-)
```
→ 뭘 시켰나: 비교 기준(origin/main)과 현재 상태(HEAD) 사이 바뀐 파일 목록을 뽑았습니다.
→ 뭐가 나왔나: 5개 파일만 바뀌었고, HumanSearch 제품 코드나 이슈 #22 관련 파일은 없습니다.
→ 좋은 소식: 요청받은 범위 밖 파일이 하나도 섞이지 않았습니다.

```
$ gh issue view 22 --json title,state
{"state":"OPEN","title":"현재 커밋 설명문·파일 경로의 로컬 금지값도 차단한다"}
```
→ 뭘 시켰나: 이번 변경과 섞여서는 안 되는 별도 이슈 #22의 실제 제목과 상태를 GitHub에서 직접 조회했습니다.
→ 뭐가 나왔나: 아직 열려 있고("OPEN"), 제목이 "커밋 설명문·파일 경로" 확장 작업임을 확인했습니다.
→ 좋은 소식: 이번 변경이 다루는 "직접 참조가 가리키는 blob 내용"과는 다른 범위입니다. 실제로 이번 diff의 5-c 구간(`scripts/acceptance-0-2.sh:149` — `[ "$reachable_type" = blob ] || continue`, 도달 가능 객체 중 blob이 아니면 건너뛰는 줄)은 여전히 blob만 검사하고 커밋/트리/태그는 검사하지 않으므로, 이슈 #22의 작업을 앞질러 하지 않았음을 코드로도 확인했습니다.

```
$ gh pr view 23 --json title,state,mergedAt
{"mergedAt":"2026-08-17T17:42:18Z","state":"MERGED","title":"시작 검사가 무해한 Git 흔적을 위험으로 오판하지 않게 한다"}
```
→ 뭘 시켰나: 문서가 전제로 삼은 선행 PR #23의 실제 병합 여부를 조회했습니다.
→ 뭐가 나왔나: 2026-08-17에 실제로 병합됨을 확인했습니다.
→ 좋은 소식: 문서의 전제가 사실과 일치합니다.

```
$ gh issue view 27 --json title,state
{"state":"OPEN","title":"도달 가능한 대용량 blob의 금지값 가짜 합격을 막는다"}
```
→ 뭘 시켰나: 이번 변경이 실제로 대응하는 이슈 #27의 제목·상태를 조회했습니다.
→ 뭐가 나왔나: 제목이 이번 변경의 목적(도달 가능한 대용량 blob의 가짜 합격 차단)과 정확히 일치합니다.

## 2) 결함 재현 — 수정 전 패턴이 실제로 "값을 찾고도 실패"

```
$ cd /tmp/sigpipe-demo && git init -q .
$ { printf 'CANARY-XYZ\n'; dd if=/dev/zero bs=1048576 count=50 2>/dev/null; } | git hash-object -w --stdin
dbb0c5fee9c5e76e2d52baba3def2cd01e3d0ff3
$ sha=dbb0c5fee9c5e76e2d52baba3def2cd01e3d0ff3
$ echo "sha=$sha"
sha=dbb0c5fee9c5e76e2d52baba3def2cd01e3d0ff3
```
→ 뭘 시켰나: 50MiB(1048576바이트 × 50블록) 크기에 맨 첫 줄에 금지값("CANARY-XYZ")이 들어간 합성 블롭을 별도 임시 저장소에 직접 만들었습니다. 객체 지문(SHA-1)은 `dbb0c5fee9c5e76e2d52baba3def2cd01e3d0ff3` 전체 40자리입니다.

```
$ echo "== OLD buggy pattern (grep -qF, no set -o pipefail effect shown) =="
== OLD buggy pattern (grep -qF, no set -o pipefail effect shown) ==
$ if git cat-file blob "$sha" 2>/dev/null | grep -qF "CANARY-XYZ"; then echo "grep matched, rc of whole expr = 0"; else echo "grep rc nonzero"; fi
grep rc nonzero
$ echo "exit code of pipeline with pipefail:"
exit code of pipeline with pipefail:
$ bash -c 'set -o pipefail; git cat-file blob '"$sha"' 2>/dev/null | grep -qF "CANARY-XYZ"; echo "pipeline_rc=$?"'
pipeline_rc=141
```
→ 뭘 시켰나: origin/main에 있던(수정 전) 검사 방식(`grep -qF`, 값을 찾으면 즉시 읽기를 끊는 옵션)을 SHA `dbb0c5fee9c5e76e2d52baba3def2cd01e3d0ff3` 블롭에 그대로 재현했습니다.
→ 뭐가 나왔나: `grep`은 값을 정확히 찾았지만(내부적으로는 발견 성공), 앞 단계인 `git cat-file`이 아직 다 쓰지 못한 나머지 데이터를 두고 파이프가 먼저 닫혀 신호로 강제 종료(종료 코드 `pipeline_rc=141`)됩니다. 이 141이라는 값이 전체 판정 조건문으로 들어가면 "실패"가 아니라 "그냥 조건이 참이 아니었다"로 처리되어, 실제로는 값이 있었는데도 통과합니다.
→ 나쁜 소식이었던 것(수정 전): 이것이 바로 이번 작업이 고치려던 실제 버그이며, 문서의 주장이 아니라 제가 직접 눈으로 확인한 사실입니다.

```
$ echo "== NEW pattern (grep -aF, no -q, full read) =="
== NEW pattern (grep -aF, no -q, full read) ==
$ bash -c 'set -o pipefail
if git cat-file blob '"$sha"' 2>/dev/null | LC_ALL=C grep -aF "CANARY-XYZ" >/dev/null; then
  st=("${PIPESTATUS[@]}")
else
  st=("${PIPESTATUS[@]}")
fi
echo "cat_file_rc=${st[0]} grep_rc=${st[1]}"'
cat_file_rc=0 grep_rc=0
$ echo "== timing comparison =="
== timing comparison ==
$ time (git cat-file blob "$sha" 2>/dev/null | LC_ALL=C grep -aF "CANARY-XYZ" >/dev/null)
( git cat-file blob "$sha" 2> /dev/null | LC_ALL=C grep -aF "CANARY-XYZ" > ; )  0.03s user 0.04s system 7% cpu 0.880 total
```
→ 뭘 시켰나: 이번 수정이 채택한 방식(`-q` 옵션을 빼서 끝까지 읽게 하고, 앞뒤 두 명령의 종료 코드를 각각 따로 확인)을 같은 SHA `dbb0c5fee9c5e76e2d52baba3def2cd01e3d0ff3` 블롭에 재현했습니다.
→ 뭐가 나왔나: 앞 단계도 정상 종료(`cat_file_rc=0`), 검색도 정상적으로 값을 찾음(`grep_rc=0`)으로 정확히 구분됩니다. 전체 실행 시간은 0.88초로 미미합니다.
→ 좋은 소식: 수정 후에는 신호로 인한 오판 여지가 사라졌습니다. `scripts/acceptance-0-2.sh:151-168`(같은 방식을 쓰는 실제 코드, 도달 가능 blob 검사 구간)과 `.github/workflows/verify.yml:80-97`(서버 쪽 동일 로직)이 이 패턴을 그대로 씁니다.

## 3) 20개 사례 실행 결과 (실측 전문)

```
$ time bash scripts/acceptance-0-2-unreachable-content.sh 2>&1; echo "EXIT=$?"
[1/20] SECRET_PATTERNS_FILE=/dev/null 상속을 격리 -> PASS (exit=0)
[2/20] SECRET_PATTERNS_FILE=.secret-patterns.default 상속을 격리 -> PASS (exit=0)
[3/20] 일반 실행은 무해한 unreachable blob을 허용 -> PASS (exit=0)
[4/20] 일반 실행은 금지값이 든 unreachable blob을 차단 -> BLOCKED (exit=1)
[5/20] unreachable commit message의 금지값을 차단 -> BLOCKED (exit=1)
[6/20] unreachable tree path의 금지값을 차단 -> BLOCKED (exit=1)
[7/20] unreachable annotated tag message의 금지값을 차단 -> BLOCKED (exit=1)
[8/20] git fsck 실패는 검사 대상 없음으로 통과하지 않음 -> BLOCKED (exit=1)
[9/20] unreachable 객체 읽기 실패는 조용히 통과하지 않음 -> BLOCKED (exit=1)
[10/20] 알 수 없는 unreachable 객체형은 읽기 실패로 차단 -> BLOCKED (exit=1)
[11/20] 큰 unreachable blob 앞쪽의 금지값도 차단 -> BLOCKED (exit=1)
[12/20] 직접 참조가 가리키는 작은 blob의 금지값을 차단 -> BLOCKED (exit=1)
[13/20] 직접 참조가 가리키는 50MiB blob 앞쪽의 금지값을 차단 -> BLOCKED (exit=1)
[14/20] 도달 가능한 blob 읽기 실패를 값 없음으로 통과하지 않음 -> BLOCKED (exit=1)
[15/20] 서버 본문도 직접 참조의 작은 blob 금지값을 차단 -> BLOCKED (exit=1)
[16/20] 서버 본문도 직접 참조의 50MiB blob 금지값을 차단 -> BLOCKED (exit=1)
[17/20] 서버 본문도 blob 읽기 실패를 값 없음으로 통과하지 않음 -> BLOCKED (exit=1)
[18/20] 종료상태 실행은 무해한 unreachable blob도 차단 -> BLOCKED (exit=1)
[19/20] 예정 사례 수와 실제 실행 수가 다르면 전체를 차단 -> BLOCKED (exit=1)
[20/20] Git hook 환경에서도 바깥 저장소 무오염 -> PASS (exit=0)
CHECKED: 20
PASS: AC-19 일반 내용 검사와 종료상태 0건 조건 분리
bash scripts/acceptance-0-2-unreachable-content.sh 2>&1  15.95s user 15.59s system 54% cpu 57.942 total
EXIT=0
```
→ 뭘 시켰나: 20개 사례(환경 변수 오염 차단 2건, 무해한 객체 통과 1건, 도달 불가 객체 4종 차단, 도구 자체 실패 2건, 알 수 없는 객체 종류 1건, 대용량 도달 불가 객체 1건, 도달 가능 작은/큰 blob 2건, 도달 가능 blob 읽기 실패 1건, 서버 쪽 동일 3건, 종료상태 검사 1건, 사례 수 불일치 차단 1건, 검사 스크립트 실행이 바깥 저장소를 건드리지 않는지 1건)를 전부 실행했습니다.
→ 뭐가 나왔나: 20개 전부 예정된 문구·예정된 성공/실패로 일치했고, `CHECKED: 20`, 최종 판정 `PASS: AC-19 일반 내용 검사와 종료상태 0건 조건 분리`, 전체 종료 코드 `EXIT=0`을 확인했습니다. 실행 시간은 57.942초입니다.
→ 좋은 소식: 기존 보호(작은 blob, 커밋 설명문, 파일경로, 태그, fsck 실패, 읽기 실패, 알 수 없는 타입, 종료상태, 바깥 저장소 무오염)와 새 보호(큰 도달 가능 blob, 그 읽기 실패, 서버 쪽 동일 항목, 사례 수 불일치 차단) 모두 실제로 동작합니다.

## 4) RED 커밋과 현재 상태의 시험 파일 동일성

```
$ git diff ae0f75b..HEAD -- scripts/acceptance-0-2-unreachable-content.sh
(출력 없음)
```
→ 뭘 시켰나: 처음 "일부러 실패하게 고정한" 커밋(전체 해시 `ae0f75b33a88cfca5a06641016e391f140ef8d03`)과 지금(`HEAD` = `f64e4e554c1b56b4b60354326a939f3c8483d68f`) 사이에서 시험 파일이 바뀌었는지 비교했습니다.
→ 뭐가 나왔나: 한 글자도 바뀌지 않았습니다(빈 diff).
→ 좋은 소식: "판정기(대상 코드)만 고치고 시험 자체는 손대지 않는다"는 규율이 실제로 지켜졌습니다. 이는 "테스트를 고쳐서 통과시킨 척"하는 흔한 눈속임을 배제합니다.

관련 커밋 4개의 전체 해시와 메시지 요약:
- `ae0f75b33a88cfca5a06641016e391f140ef8d03` — "대용량 도달 객체의 가짜 합격을 실패 시험으로 고정한다" (RED 고정, `Tested: bash-n; RED 20 cases with 5 expected failures`)
- `1b0b73e0678c8f6929982b24835713a353265fde` — "큰 도달 객체의 금지값을 검사 두 곳에서 놓치지 않게 한다" (GREEN 수리, `Tested: bash-n; AC-19 20/20; inner hook 16/16; RED test immutable`)
- `f64e4e554c1b56b4b60354326a939f3c8483d68f` — "실제 경로와 변조 시험의 판정 근거를 보존한다" (`Tested: actual acceptance-0-2 exit 0; exact reachable mutation exit 1; restoration diff 0`)

## 5) 세션 상태 점검의 "20개 중 1개 실패" 원인 추적

```
$ git status
On branch task/gate0-reachable-large-blob-sigpipe
Your branch is ahead of 'origin/main' by 4 commits.
  (use "git push" to publish your local commits)

nothing to commit, working tree clean
```
```
$ bash scripts/session-status.sh 2>&1 | tail -40
HEAD: f64e4e5 (ahead 4 / behind 0)
ORIGIN: 34e4ccf
RED: 1/20 (acceptance-0-7.sh 제외 — CI 담당)

[exited with code 0]
```
→ 뭘 시켰나: 저장소 전체의 인수 검사 20개(0-2 대용량 blob 검사만이 아니라 저장소 전체의 검사 스크립트 20개, 우연히 같은 숫자 20)를 전부 돌려 몇 개가 실패하는지 확인했습니다.
→ 뭐가 나왔나: `RED: 1/20` — 1개 실패.

```
$ SECRET_PATTERNS_FILE= bash scripts/acceptance-0-5.sh
FAIL: origin/main(34e4ccff88ce776d06e3001734005a34b169f7eb) != main(1e0f805652e114ff7104cd6c9fd9394422ea514f) — push 미완료
```
→ 뭘 시켰나: 저장소 안의 20개 검사 스크립트를 하나씩 개별 실행해 실패한 것이 정확히 무엇인지 짚어냈습니다.
→ 뭐가 나왔나: `scripts/acceptance-0-5.sh`(push+CI 연결 검사)만 실패했고, 실패 사유는 원격 `origin/main`(전체 해시 `34e4ccff88ce776d06e3001734005a34b169f7eb`)과 로컬 `main`(전체 해시 `1e0f805652e114ff7104cd6c9fd9394422ea514f`)이 서로 다르다는 것입니다.

```
$ git log --oneline -3 main
1e0f805 docs(engineering): P0 보완판 — 샌드박스 면제 범위 4파일 전체로 확대(밖 검증 실측 근거) + 재개 지점 S4 Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
8cd1e9e docs(engineering): P0 보완판 — 재개 지점을 실커밋 기준으로 갱신 Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
34e4ccf docs(engineering): P0 보완판 — 부하 민감 판단 정정, ps 차단=샌드박스 제약 확정 Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
$ git rev-list --left-right --count origin/main...main
0	2
```
→ 뭘 시켰나: 로컬 `main` 브랜치와 원격 `origin/main`이 왜 다른지 커밋 내용과 개수로 확인했습니다.
→ 뭐가 나왔나: 로컬 `main`이 원격보다 커밋 2개(`1e0f805`, `8cd1e9e`) 앞서 있습니다. 두 커밋 모두 제목이 "P0 보완판" 문서 정리 작업이며, 이번 검토 대상인 큰 blob 결함 수정과는 제목·내용 모두 무관합니다.
→ 좋은 소식이자 바로잡을 점: 이 실패는 이번 변경의 결함이 아니라 별도 작업이 아직 원격에 올리지 않은 커밋 때문입니다. 다만 원인 커밋 수는 문서가 암시한 "1개"가 아니라 "2개"였습니다.

## 6) 문서(goal 문서)의 완료 주장 여부

`docs/engineering/gate0-reachable-large-blob-sigpipe-goal-2026-08-18.md:4` — "같은 변경이 서버에서도 성공하기 전에는 다음 HumanSearch 선행 작업을 시작하지 않습니다." (다음 작업을 서버 성공 확인 뒤로 명시적으로 미룸)
`…:394` (8-4절) — "이 절 아래에 문법, session-status, verify, pre-push 전체 결과를 순서대로 추가합니다." (아직 채워지지 않은 빈 자리)
`…:398` (9절) — "이 절 아래에 실제 `claude -p` 명령 전문과 판정 원문, Codex V2 재현 명령·출력·일치 여부를 순서대로 추가합니다." (적대적 검증 기록도 아직 빈 자리)
→ 뭘 시켰나: 문서에서 "다 끝났다"는 표현이 있는지, 아니면 남은 절차를 스스로 인정하고 있는지 읽었습니다.
→ 뭐가 나왔나: 완료를 주장하는 문장은 없고, 오히려 "전체 게이트 통과 기록"과 "적대 검증 로그"를 비워 둔 채 앞으로 채울 자리로 남겨 두었습니다.
→ 판단: 과장된 완료 주장 없음 — PASS 판정에 영향 없음.

## 7) SOT 문서 수치 일치

```
$ git diff origin/main..HEAD -- docs/sot/verification-commands.md
-**워크플로 스텝 17개 전부**를 적는다(...) 아래는 `verify.yml` 의 `- name:` 스텝 순서 그대로다(#6·#9 G1·#11 G2·#8 AC-M 병합 후 합집합과 #19 AC-19의 13개 합성 사례 — 2026-08-17).
+**워크플로 스텝 17개 전부**를 적는다(...) 아래는 `verify.yml` 의 `- name:` 스텝 순서 그대로다(#6·#9 G1·#11 G2·#8 AC-M 병합 후 합집합과 #19·#27 AC-19의 20개 합성 사례 — 2026-08-18).
...
-| 5 | 인수 검사 0-2 상시/종료상태 분리 | `bash scripts/acceptance-0-2-unreachable-content.sh` — 환경 격리·네 객체형·도구 실패·큰 객체·종료상태·훅 환경 무오염 13개 합성 사례 (AC-19) |
+| 5 | 인수 검사 0-2 상시/종료상태 분리 | `bash scripts/acceptance-0-2-unreachable-content.sh` — 환경 격리·네 객체형·도구 실패·도달 여부별 큰 객체·정확한 차단 이유·사례 수·종료상태·훅 환경 무오염 20개 합성 사례 (AC-19) |
```
→ 뭘 시켰나: SOT 문서가 적은 사례 수("13개"→"20개")가 실제 실행 결과와 맞는지 diff로 대조했습니다.
→ 뭐가 나왔나: 실행해서 나온 실제 숫자(20개, 전부 통과, 위 3항목)와 문서 숫자가 일치합니다. 이전 판(origin/main)의 "13개"라는 옛 숫자도 함께 갱신되어 있습니다.

## 8) macOS bash 3.2 환경 확인

```
$ bash --version | head -1
GNU bash, version 3.2.57(1)-release (arm64-apple-darwin24)
$ which bash
/bin/bash
$ bash -n scripts/acceptance-0-2.sh && echo "syntax OK acceptance-0-2.sh"
syntax OK acceptance-0-2.sh
$ bash -n scripts/acceptance-0-2-unreachable-content.sh && echo "syntax OK unreachable-content.sh"
syntax OK unreachable-content.sh
```
→ 뭘 시켰나: 실제 이 컴퓨터가 쓰는, 비교적 오래된 bash 버전(3.2.57)에서 문법 오류와 실행 결과를 확인했습니다.
→ 뭐가 나왔나: 문법 오류 없음, 20/20 통과(위 3번 항목). `PIPESTATUS` 배열, `set -o pipefail`, 조건문 안에서의 오류 무시 등 이번 수정이 쓰는 기법은 이 오래된 bash에서도 정상 동작합니다.

## 9) CI 워크플로 셸 실행 환경 확인

`.github/workflows/verify.yml:16` — `runs-on: ubuntu-latest`, 별도의 `shell:` 지정 없음.
→ GitHub Actions는 `shell:`을 명시하지 않은 Linux 러너의 `run:` 단계를 기본적으로 `bash --noprofile --norc -eo pipefail {0}`(=bash, sh 아님)로 실행합니다. 따라서 이번 수정이 쓰는 `PIPESTATUS` 배열 문법이 서버에서도 문법적으로는 정상 지원되는 환경입니다. (다만 위 "확인하지 못한 것"에 적었듯, 실제 서버 실행 자체는 이번 검토에서 하지 않았습니다.)

## 10) 저장소 오염 여부

```
$ git status --porcelain; echo "clean_check_rc=$?"
clean_check_rc=0
```
→ 뭘 시켰나: 리뷰 작업(별도 `/tmp/sigpipe-demo` 임시 폴더에서만 재현 실험 수행) 이후 검토 대상 저장소 자체에 변경이 남았는지 확인했습니다.
→ 뭐가 나왔나: 변경 사항 없음(빈 출력, 종료 코드 0).
→ 좋은 소식: 이번 검토 과정에서 검토 대상 저장소를 오염시키지 않았습니다.
