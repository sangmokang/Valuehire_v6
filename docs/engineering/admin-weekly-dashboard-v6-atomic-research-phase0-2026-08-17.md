# Admin Weekly Dashboard v6 atomic research — phase0 — 2026-08-17

이 파일은 fresh read-only 조사자의 원문 증거입니다. 정본 계획이 아니며 controller의 수정 규칙과 독립 계획 감사를 우선합니다.

## Metis Analysis: AC-01/02/03/39 원자 분해

결론: 파일 쓰기 0건, 외부 호출 0건, 하위 에이전트 0건. 기준 HEAD는 `a02a3da8f36e22997028b0d620de4e7e970f76d8`로 재확인했고 작업트리 출력은 비어 있었습니다.

근거 핵심: 주입/저장소 AGENTS가 최상위 정본이라는 목표 계약은 `docs/engineering/admin-weekly-dashboard-v6-replacement-goal-2026-08-16.md:82-93`, 새 admin 검사 4종 묶음은 `:175-181`, acceptance receipt 계약은 `:184-190`, admin data exposure 계약은 `:192`, AC 본문은 `:1160-1162` 및 `:1198`입니다. 현재 `apps/admin`, 루트 `package.json`, `pnpm-lock.yaml`, `.node-version`은 존재하지 않았고, `scripts/scan-data-exposure.sh`는 현재 `tracked|history|pii|all`만 허용합니다(`scripts/scan-data-exposure.sh:30-33`).

### Micro 후보

| parent_ac | micro_id | single_observable_result | single_failure_reason | rollback_unit | dependencies | allowed_files | forbidden_scope | red_command | green_command | mutation_method | production_call_path | target_count_method | cannot_split_reason | external_side_effect_count_expected |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| AC-01 | AC01-M1-clean-install | clean checkout에서 고정 Node/pnpm 계약으로 `pnpm install --frozen-lockfile`이 성공하고 실제 workspace package 수를 출력 | 버전 불일치, lockfile 누락, package 해석 실패는 `VERSION_CONTRACT_FAIL` | root workspace + lockfile | 목표 고정 버전 `:196-220` | `.node-version`, `package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`, `apps/admin/package.json` | v4/v5 import, Python humansearch 수정, 임의 버전 변경 | `bash scripts/acceptance-admin-foundation.sh` | `pnpm install --frozen-lockfile` 및 foundation receipt PASS | `.node-version` 또는 `packageManager`를 문서와 다르게 변경 | clean checkout -> root workspace install | `pnpm list --depth -1` 또는 acceptance receipt `targetCount>=1` | 더 쪼개면 단일 install 계약의 readback이 분리됨 | 0 |
| AC-01 | AC01-M2-eslint | admin ESLint가 `eslint . --max-warnings=0`으로 실행되고 검사 파일 수 1 이상 | `next lint` 사용, 검사 대상 0건, warning 허용 | admin lint config/package scripts | AC01-M1 | `apps/admin/eslint.config.*`, `apps/admin/package.json`, admin source | UI 기능 구현, DB/auth/connector | `bash scripts/acceptance-admin-foundation.sh` | `pnpm --filter admin lint` | lint 대상 glob을 빈 경로로 바꿈 | clean checkout -> admin lint command | ESLint output 또는 wrapper가 checked file count 수집 | N/A | 0 |
| AC-01 | AC01-M3-typecheck | admin TypeScript strict typecheck가 실행되고 대상 TS 파일 수 1 이상 | `tsconfig` 완화, 대상 0건, typecheck script 누락 | admin tsconfig/package scripts | AC01-M1 | `apps/admin/tsconfig.json`, `apps/admin/src/**`, `apps/admin/package.json` | DB migration, connector network | `bash scripts/acceptance-admin-foundation.sh` | `pnpm --filter admin typecheck` | `strict:false` 또는 include 빈 배열 | clean checkout -> tsc/noEmit | `tsc --listFilesOnly` count 또는 receipt targetCount | N/A | 0 |
| AC-01 | AC01-M4-unit-test | Vitest가 실행되고 테스트 케이스 수 1 이상 | test 0건, runtime import 없이 텍스트 단언만 통과 | admin unit test skeleton | AC01-M1 | `apps/admin/tests/unit/**`, `apps/admin/src/**`, `apps/admin/package.json` | e2e/UI visual, DB 통합 | `bash scripts/acceptance-admin-foundation.sh` | `pnpm --filter admin test` | 테스트 파일 제외 또는 `.skip` 처리 | clean checkout -> Vitest | Vitest json/reporter test count | N/A | 0 |
| AC-01 | AC01-M5-build | Next build가 성공하고 `.next` 산출물 파일 수 1 이상 | build 대상 0건, Next app 누락, 형제 저장소 필요 | admin Next app skeleton | AC01-M1~M4 | `apps/admin/next.config.ts`, `apps/admin/src/app/**`, `apps/admin/package.json` | legacy asset copy, iframe old dashboard | `bash scripts/acceptance-admin-foundation.sh` | `pnpm --filter admin build` | route 파일 제거 또는 build script no-op | clean checkout -> Next build | `.next` file count after build | N/A | 0 |
| AC-01 | AC01-M6-start-smoke | build 후 admin start가 로컬에서 뜨고 health/page smoke 1건이 응답 | start script 없음, 서버가 부팅 실패, sibling repo 요구 | admin start script + minimal route | AC01-M5 | `apps/admin/src/app/**`, `apps/admin/package.json`, `apps/admin/next.config.ts` | 배포/운영 주소 연결 | `bash scripts/acceptance-admin-foundation.sh` | `pnpm --filter admin start` smoke under acceptance | start script를 `echo ok`로 대체 | clean checkout -> start -> local HTTP smoke | smoke response count 1 | N/A | 0 |
| AC-02 | AC02-M1-registration-bundle | 새 `scripts/acceptance-admin-*.sh` 집합이 pre-push glob, CI 고정 목록, verification SOT, mechanism registry에 모두 등록되고 하나 제거 mutation이 RED | 로컬에만 있는 검사, CI에만 있는 검사, 명부 dead target | admin acceptance registration bundle | AC01 기반 | `scripts/acceptance-admin-*.sh`, `.github/workflows/verify.yml`, `docs/sot/verification-commands.md`, `docs/sot/mechanism-registry.yaml` | `hooks/pre-push` 재작성, 기존 hs/0-* 검사 약화 | `bash scripts/acceptance-admin-foundation.sh` after deleting one CI/admin line in temp copy | `bash scripts/acceptance-admin-foundation.sh && bash scripts/acceptance-verify-ac-m.sh` | temp fixture에서 CI run line 또는 registry entry 제거 | pre-push find glob `hooks/pre-push:110-165` + CI `verify.yml` | compare `scripts/acceptance-admin-*.sh` set vs CI admin run lines; registry CHECKED count | 사용자 지시상 scripts + CI + 두 SOT + CI/admin mutation은 분할 금지 묶음 | 0 |
| AC-03 | AC03-M1-runtime-graph-clean | runtime dependency graph에 v4/v5 경로·module·DB endpoint 0건 | runtime import 또는 config가 legacy path/endpoint 참조 | dependency graph scanner/test | AC01-M1~M4 | `apps/admin/src/**`, `apps/admin/package.json`, `contracts/cleanroom-deny-patterns.txt` 또는 admin contract | build output 검사, UI visual | `bash scripts/acceptance-admin-foundation.sh` 또는 dedicated admin cleanroom acceptance | dependency graph scanner PASS | `apps/admin/src`에 `../../Valuehire_v5` import fixture | admin runtime imports -> graph scan | graph node/edge count and deny-hit count | N/A | 0 |
| AC-03 | AC03-M2-build-output-clean | build output에 v4/v5 path/module/asset/DB endpoint 0건 | `.next`에 legacy string/asset reference 포함 | build output scanner | AC01-M5 | `apps/admin/.next` generated by build, scanner script/acceptance | runtime graph logic, source refactor beyond violation | `bash scripts/acceptance-admin-foundation.sh` with injected legacy marker in build artifact temp copy | `pnpm --filter admin build && bash scripts/acceptance-admin-foundation.sh` | temp `.next` file에 `Valuehire_v4` canary 삽입 | admin build -> artifact scan | scanned artifact file count and deny-hit count | N/A | 0 |
| AC-39 | AC39-M1-admin-mode-build-root-canary | `bash scripts/scan-data-exposure.sh admin`이 `apps/admin/.next` canary를 잡아 exit 1 | build root 누락 또는 canary 미탐 | admin scan mode | AC01-M5 | `scripts/scan-data-exposure.sh`, admin acceptance fixture | existing `all` semantics 약화 | `bash scripts/scan-data-exposure.sh admin` in temp fixture with canary | same command clean fixture PASS | temp `.next`에 PII/log canary 생성 | build/test/log artifact scan only | scanned `.next` file count | N/A | 0 |
| AC-39 | AC39-M2-test-results-canary | admin scanner가 `apps/admin/test-results` canary를 잡아 exit 1 | test-results root 누락 | admin scan mode | AC01-M4 | `scripts/scan-data-exposure.sh`, admin acceptance fixture | Playwright/coverage/log root 판정 | same with test-results canary | same clean fixture PASS | temp `test-results` canary | artifact scan | scanned test-results count | N/A | 0 |
| AC-39 | AC39-M3-playwright-report-canary | admin scanner가 `apps/admin/playwright-report` canary를 잡아 exit 1 | playwright report root 누락 | admin scan mode | Playwright artifact root exists or fixture creates it | `scripts/scan-data-exposure.sh`, admin acceptance fixture | test-results/coverage/log root 판정 | same with playwright-report canary | same clean fixture PASS | temp `playwright-report` canary | artifact scan | scanned playwright-report count | N/A | 0 |
| AC-39 | AC39-M4-coverage-canary | admin scanner가 `apps/admin/coverage` canary를 잡아 exit 1 | coverage root 누락 | admin scan mode | test/build may create coverage or fixture creates it | `scripts/scan-data-exposure.sh`, admin acceptance fixture | other artifact roots | same with coverage canary | same clean fixture PASS | temp coverage canary | artifact scan | scanned coverage count | N/A | 0 |
| AC-39 | AC39-M5-admin-log-root-canary | admin scanner가 admin application log root canary를 잡아 exit 1 | log root 정의 누락 또는 미스캔 | admin log root contract + scanner | admin log root path 결정 필요 | `scripts/scan-data-exposure.sh`, admin runtime/log config, acceptance fixture | external logging service, production logs | same with log-root canary | same clean fixture PASS | temp admin log root canary | local artifact/log scan | scanned log file count | N/A | 0 |
| AC-39 | AC39-M6-empty-root-not-run | admin target root 또는 파일 수 0건이면 exit 2 + `NOT_RUN` | 빈 디렉터리/없는 root를 PASS 처리 | admin scanner target counting | admin mode exists | `scripts/scan-data-exposure.sh`, acceptance fixture | canary content detection | temp fixture with no admin roots/files | fixture with roots/files and no canary PASS | remove all target roots/files in temp fixture | scanner preflight | target root count and file count | N/A | 0 |
| AC-39 | AC39-M7-admin-scan-registration-bundle | `bash scripts/scan-data-exposure.sh admin`이 acceptance, pre-push, CI, verification SOT, mechanism registry에서 실제 실행된다 | admin scan이 로컬 전용이거나 SOT/registry dead target | admin scan registration bundle | AC39-M1~M6 | `scripts/acceptance-admin-*.sh`, `.github/workflows/verify.yml`, `docs/sot/verification-commands.md`, `docs/sot/mechanism-registry.yaml` | `hooks/pre-push` 재작성, 기존 `all` scan 제거 | temp CI/registry/admin acceptance line deletion mutation | `bash scripts/acceptance-admin-foundation.sh && bash scripts/acceptance-verify-ac-m.sh` | CI `run: bash scripts/scan-data-exposure.sh admin` 제거 | acceptance -> pre-push glob -> CI -> registry checker | registry CHECKED and CI active run line count | 사용자 지시상 acceptance + CI + 두 SOT + mutation은 분할 금지 묶음 | 0 |

### Open Questions

- [ ] admin application log root의 정확한 저장소 상대 경로를 무엇으로 고정할지 결정 필요 — AC-39-M5/M6의 target root count가 이 값 없이는 구현자마다 달라질 수 있음.
- [ ] AC-01 `start` smoke의 허용 포트/health URL을 고정해야 함 — 랜덤 포트 허용 여부와 smoke 대상이 없으면 acceptance가 주관적으로 변함.
- [ ] AC-03 deny pattern의 정본을 `contracts/cleanroom-deny-patterns.txt`로 확장할지 별도 admin contract로 둘지 결정 필요 — runtime graph와 build output scanner가 같은 금지 목록을 써야 함.
