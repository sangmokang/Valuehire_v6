# Codeaudit 파일 크기 게이트 후속 goal — 강제력 설명 정정 (2026-08-15)

## 결론

파일 크기 검사는 실제로 작동하지만, 목표 문서가 서버 검사를 마지막 차단선이라고 잘못 설명해 현재 상태를 과장했습니다. 원격 기본선은 보호되지 않았으므로 설명을 사실대로 고치고 검증을 다시 통과시킨 뒤에만 병합 준비 상태로 보고합니다.

사장님께서 지금 결정하실 일은 없습니다. 병합과 배포는 자동 실행하지 않습니다.

## 판단 근거

- 파일 크기 본체와 자기시험은 로컬·서버 자동검사 실행 칸에 연결돼 있고 기존 서버 검사도 성공했습니다.
- 그러나 원격 기본선 조회 결과는 `protected=false`였고, 보호 규칙과 저장소 규칙 조회는 현재 요금제에서 403으로 거부됐습니다. 따라서 실패 표시는 생겨도 합치기 자체가 자동으로 막히지 않습니다.
- 로컬 전송 전 검사는 `git push --no-verify`로 우회할 수 있습니다. 현재 강제력은 사람 규율과 실패 표시까지이며, 문서가 말한 “마지막 차단선”은 존재하지 않습니다.

> **무엇을** — 파일 크기 목표 문서의 강제력 설명을 원격 실측과 로컬 우회 한계에 맞게 고칩니다.
> **왜** — 검사가 합치기를 막는다고 믿으면 사람 검토를 줄이는 잘못된 결정을 할 수 있기 때문입니다.
> **버린 길** — “서버 검사가 중요하다”처럼 표현만 약하게 바꾸는 길은 실제 차단 권한이 없다는 핵심을 숨기므로 버렸습니다.
> **대가** — 문서가 길어지고, 계정 요금제나 저장소 공개 범위가 바뀌면 원격 상태를 다시 확인해야 합니다.
> **되돌리기** — 이 문서 정정 커밋만 되돌릴 수 있지만, 그러면 이미 반증된 과장 문구가 다시 살아납니다.

## 증거와 실행 계약

### 1. 현재 상태

| 확인 지점 | 현재 사실 | 사업상 의미 |
|---|---|---|
| `docs/engineering/file-size-gate-goal-2026-08-15.md:119` | “이 게이트의 최종 강제선은 서버 검사(CI)다”라고 단정합니다. | 서버가 합치기를 막는다는 잘못된 안심을 줍니다. |
| 원격 `main` 조회 | `protected=false`입니다. | 실패한 검사를 무시한 합치기를 기계적으로 거부하지 않습니다. |
| 보호 규칙·저장소 규칙 조회 | 개인 계정의 비공개 저장소 요금제 때문에 둘 다 403으로 거부됐습니다. | 현재 상태에서 필수 성공 검사를 설정할 수 없습니다. |
| `hooks/pre-push` 계약 | 로컬 검사는 `--no-verify`로 우회할 수 있습니다. | 로컬 검사도 강제 차단선이 아니라 협조적 장치입니다. |

→ 파일 크기 검사 구현은 진짜지만, “검사 존재”와 “합치기 거부 권한”은 다른 문제입니다. 이번 수정은 구현을 바꾸지 않고 이 권한 차이를 문서에 정확히 남깁니다.

### 2. 근본 원인

서버 자동검사가 성공·실패 결과를 만든다는 사실과, 실패한 변경을 합치지 못하게 막는 원격 보호 설정을 같은 것으로 취급했습니다. 보호 설정을 직접 조회하지 않은 상태에서 강제력을 단정한 것이 원인입니다.

### 3. 기계로 판정할 인수 기준

| 번호 | 합격 단언 | 가짜 합격을 깨는 반례 |
|---|---|---|
| B1 | 기존 “최종 강제선” 단정이 0건이고, 요약과 새 설명 모두 검사 실패 표시와 실제 합치기 차단 권한을 구분하며, 새 설명에 `protected=false`, 요금제 제약, 로컬 검사 우회 가능성, 강제에 필요한 선택이 함께 있습니다. | 표현만 부드럽게 바꾸거나 요약에 “기계가 막는다”는 과장을 남기거나 원격 실측·우회 한계를 빼면 불합격입니다. |
| B2 | 파일 크기 본체·자기시험·비밀 검사가 수정 뒤에도 통과합니다. | 문서 정정 과정에서 실행 파일이나 검사 연결이 달라지면 불합격입니다. |

→ 금지 문구 0건만으로 합격시키지 않습니다. 새 설명의 네 근거와 기존 검사 회귀 통과를 함께 확인합니다.

### 4. Harness 진행

- [x] 게이트 0 — `bash scripts/session-status.sh` 결과 `RED: 0/21`을 확인했습니다. `acceptance-0-7.sh`는 계약대로 서버 검사 담당이라 제외됐습니다.
- [x] 게이트 1 — 이 문서에 현재 상태, 근본 원인, 단언별 합격 조건과 반례를 남겼습니다.
- [x] 게이트 2 — 사용자 실행 문서가 지정한 기존 `task/file-size-gate` 작업 공간을 사용합니다. 열린 변경 요청 #14의 감사 후속이므로 새 변경 요청으로 분리하지 않습니다.
- [ ] 게이트 2 RED — 현재 잘못된 문구가 한 건 검출되는지 먼저 확인하고 원문을 남깁니다.
- [ ] 게이트 3 GREEN — 목표 문서 한 곳만 최소 수정합니다.
- [ ] 게이트 4 — 금지 문구 0건, 필수 근거 존재, 파일 크기 검사·자기시험·비밀 검사 통과를 확인합니다.
- [ ] 게이트 5 — Lore 형식으로 커밋하고 변경 요청 #14 브랜치에 전송한 뒤 서버 검사 성공을 확인합니다.
- [ ] 게이트 6 — 병합과 작업 공간 회수는 사장님 지시 전까지 하지 않습니다.

### 5. 적대검증 항목

- “최종”, “강제”, “막는다” 같은 동의어로 과장 문구가 남았는지 문서 전체를 다시 찾습니다.
- 서버 검사 성공과 기본선 보호를 혼동한 문장이 남았는지 공격합니다.
- 원격 조회가 바뀌었거나 다른 원격 규칙이 존재하는지 보호 규칙·저장소 규칙·기본선 상태를 교차확인합니다.
- Claude가 낸 모든 파일 위치와 실행 결과를 Codex가 같은 명령으로 다시 확인하고, PASS 과장과 FAIL 과장을 모두 의심합니다.

### 6. 읽은 정본과 준수 항목

- `docs/sot/INDEX.md` — 정본과 사건 기록의 경계를 확인했습니다.
- `docs/sot/coding-principles.md` — 로컬 검사는 우회 가능하다는 전제와 증거 없는 완료 금지를 적용합니다.
- `docs/sot/git-workflow.md` — 기본선 직접 전송 금지, 기존 작업 브랜치 사용, 자동 병합 금지를 따릅니다. 원격 상태 정정은 변경 요청 #13의 같은 감사 후속에서 한 번만 수행해 두 변경 요청 간 중복 충돌을 피합니다.
- `docs/sot/hook-contracts.md` — 로컬 전송 전 검사의 우회 한계를 그대로 밝힙니다.
- `docs/sot/verification-commands.md` — 이 저장소의 실제 셸 검증 명령을 사용합니다.

### 7. 비범위

- 파일 크기 상한 자체와 자기시험 로직은 바꾸지 않습니다.
- 같은 `git-workflow.md` 정정을 변경 요청 #13과 #14에 중복으로 넣지 않습니다. #13이 정본 수정 소유자입니다.
- 계정 요금제 변경, 저장소 공개 전환, 원격 보호 규칙 설정은 비용·공개 범위가 달라지는 별도 결정이므로 실행하지 않습니다.
- 병합, 배포, 운영 전송은 실행하지 않습니다.
- 과거 `.claude/private-reviews/` 원문은 Git 추적 제외 상태를 유지합니다. 민감정보 검토 없이 추적 파일로 옮기지 않으며, 작업 공간도 증거 회수 결정 전에는 삭제하지 않습니다.

## RED 증거

```text
$ rg -n '이 게이트의 최종 강제선은 서버 검사\(CI\)다\.' docs/engineering/file-size-gate-goal-2026-08-15.md
119:- 이 게이트의 최종 강제선은 서버 검사(CI)다. 로컬 pre-push 훅은 push하는 사람 소유 파일이라, 훅 자체를 고치거나 건너뛰는 유형(협조적 잠금의 원리적 한계 — 저장소가 `hooks/pre-push:82-85`에서 자인)은 로컬에서 완전 차단 불가. 그래서 같은 검사를 CI 실행 단계에도 배선했다(AC-FS3).
exit=0
```

→ 잘못된 단정이 실제로 한 건 검출됐고 검색 프로그램은 정상 실행됐습니다. 이 한 건을 0건으로 만드는 것이 문서 변경의 실패→통과 기준입니다.

## 적대 검증 로그

Claude 1차 판정 원문과 실행 명령, Codex 2차 재현 명령·전체 출력·일치 여부 표를 수정과 로컬 검증 뒤 이 절에 그대로 추가합니다.

### Claude 1차 PASS 뒤 반영한 낮은 위험 정정

- 요약부의 “기계가 막습니다”를 “위반을 실패로 표시하고 사람 검토가 합치기를 맡는다”로 좁혔습니다.
- 영향 반경의 “모든 push가 막힌다”를 훅을 우회하지 않은 전송 시도의 검사 실패로 좁혔습니다.
- 과거 판정 원문은 저장소 이력이 아니라 Git 추적에서 제외된 작업 공간 로컬 파일이라는 사실과, 증거 회수 결정 전 작업 공간을 삭제하지 않는 조건을 명시했습니다.
