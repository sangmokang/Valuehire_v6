# HumanSearch G3 portal-constants deny patterns — 전역 계층 (case-insensitive ERE).
# 적용: contracts/ 와 "문서 확장자(md·yaml·yml·txt·html)이며 실행 권한 없는 docs/ 파일"을 뺀
#       모든 추적 파일 (검사기 자신·scripts/·hooks/ 포함, 자기면제 없음). 그 밖의 docs/ 파일
#       (실행 코드·실행 권한 문서)은 검사 대상이다 (V1 결함 D5).
# 표현은 자기 자신과 매치하지 않게 설계한다 (cleanroom-deny-patterns.txt:1-2 방식).
# 이 계층에 "모든 URL"을 넣지 않는 이유: uv.lock 의 정당한 패키지 주소, 기존 보안 검사기의
# 시험용 주소가 오탐된다 — 그 계층은 -product.txt 가 제품 코드에만 건다.

# DOM/드라이버 locator API — 셀렉터는 코드가 아니라 데이터다 (웹 자동화 5조 1번).
# 2026-08-25 정밀화: 같은 줄에 따옴표 리터럴이 있을 때만 잡는다. `document.getElementById(id)`
# 처럼 값을 변수로 받는 호출은 "화면 위치가 코드에 박혔다"가 아니다 — 그 값이 어디선가
# 리터럴로 적히면 아래 셀렉터·XPath 규칙과 제품 계층이 그 리터럴 자체를 잡는다.
# 잔여 한계: API 호출과 리터럴을 서로 다른 줄로 쪼개면 이 규칙은 놓친다. 그 경우에도
# 리터럴 쪽이 제품 계층에서 잡히며, hardening7 이 그 분리 형태를 표본으로 고정한다.
querySelecto[r][^"'`]*["'`]
getElementsB[y][^"'`]*["'`]
getElementByI[d][^"'`]*["'`]
CSS_SELECTO[R][^"'`]*["'`]
find_elemen[t][^"'`]*["'`]
\.locato[r]\([^"'`]*["'`]
wait_for_selecto[r][^"'`]*["'`]

# 의미 기반 화면 찾기 API — Playwright get_by_* 계열 (V1 3차 F2)
get_by_ro[l]e[^"'`]*["'`]
get_by_lab[e]l[^"'`]*["'`]
get_by_test_i[d][^"'`]*["'`]
get_by_tex[t][^"'`]*["'`]
get_by_placeholde[r][^"'`]*["'`]
get_by_titl[e][^"'`]*["'`]
get_by_al[t]_text[^"'`]*["'`]

# XPath 리터럴 (// + 식별자 + [@ 속성 — 공백 변형 포함, V1 결함 D1)
//[a-z*][a-z0-9]*[[:space:]]*\[[[:space:]]*[@]

# CDP(브라우저 원격조종) 디버그 포트 — 플래그와 관례 대역(922x)
remote-debugging-por[t]
(localhos[t]|127\.0\.0\.1|0\.0\.0\.0):922[0-9]

# 채용 포털 브랜드 도메인 (dotted 형태만 — 산문 속 브랜드 언급은 잡지 않는다)
saram[i]n\.
jobkore[a]\.
linkedi[n]\.co
wanted\.co\.k[r]
incrui[t]\.co
jobplane[t]\.
