# HumanSearch G3 제품 루트 계약 — 제품 전용 금지 계층을 적용할 디렉터리 목록.
#
# 왜 데이터인가: 이 목록이 검사기 코드에 고정돼 있으면 새 제품 폴더가 생겼을 때
# 검사 자체가 적용되지 않는다(codex V1 D3). 목록은 데이터로 두고, 저장소에서 발견한
# 제품 코드 폴더가 여기 없으면 검사기는 조용히 통과하지 않고 exit 2(검사 불능)로 멈춘다.
# 즉 "새 폴더 등재를 잊는 것"이 통과가 아니라 실패가 된다.
#
# 한 줄에 하나. 접두 경로로 해석한다(`humansearch/src` 는 그 아래 전부).
# 항목을 지우면 그 폴더의 제품 파일이 다시 발견돼 exit 2 가 된다 — 몰래 축소할 수 없다.
humansearch/src
humansearch/tests
apps/admin
