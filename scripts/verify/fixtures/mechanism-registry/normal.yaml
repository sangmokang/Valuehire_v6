# fixture: 정상 명부 — 검사기가 exit 0 을 내야 한다 (AC-M fixture 1/3)
# path·target 은 실제 저장소에 존재하는 것을 쓴다 (2026-08-12 grep -n 실측).
- id: "secrets-scan-precommit"
  path: "hooks/pre-commit"
  target: "SECRET_PATTERNS_FILE= VERIFY_SCAN_SOURCE=index bash verify.sh"
  stage: "pre-commit"
  required: true
- id: "ci-secret-scan"
  path: ".github/workflows/verify.yml"
  target: "run: bash verify.sh"
  stage: "ci"
  ci_mirror_job: "verify"
  required: true
