# pii-allowlist.txt — acceptance-hs-1305-pii.sh 의 이메일 검사(②)에서만 통째로 빼는 파일 목록.
#
# 규칙(검사기가 강제한다):
#   · 한 줄에 저장소 루트 기준 상대경로 1개. `#` 로 시작하는 줄과 빈 줄은 주석.
#   · `humansearch/`·`contracts/`·`scripts/` 는 넣을 수 없다 — 후보 데이터가 실제로
#     들어갈 수 있는 자리라서, 여기를 통째로 빼면 게이트가 자기 목적을 잃는다.
#   · 파일 통째 면제이므로, 넣는 순간 그 파일의 **미래** PII 도 안 잡힌다.
#     (2026-09-09 정본: 유예를 기본 처리로 삼지 말 것)
#
# 현재 항목 0개. 처음 실행에서 걸린 주소는 전부 RFC 2606/6761 예약 도메인
# (`example.invalid`·`example.test`)이었고, 그것들은 파일 면제가 아니라
# 주소 규칙(검사기의 ALLOWED_EMAIL 패턴)으로 풀었다 — 사람에게 도달할 수 없는
# 도메인이라 PII 가 아니고, 주소 규칙은 파일을 눈멀게 하지 않기 때문이다.
# 그 파일들: docs/engineering/strict-principles-v1-verdict-2026-08-20.md,
# docs/engineering/strict-principles-v1-recheck-prompt-2026-08-20.md,
# humansearch/tests/test_admin_*.py(3), scripts/*.sh(4).
